# 上游套餐知识库 & 官方校对体系 — 任务总账（TASKS.md）

> **跨会话任务持久化账本。** AI 助手 / 开发者在任何新会话（含 VSCode 重启后）必须**先完整阅读本文件**，再按「当前执行指针」继续推进；每完成一项就勾选 `[x]` 并在文末「变更记录」追加一行。本文件是需求的一部分：把上游 AI 厂商的 Token Plan / Coding Plan 套餐、折扣、积分、模型上下架、活动期限全部纳入平台知识库，做到**每 6 小时自动抓取官方页面 → 与预存快照校对 → 变更进入人工确认闭环**。

---

## 0. 恢复协议（新会话必读）

1. 读本文件全文，重点看「当前执行指针」与最新「变更记录」。
2. 读 `docs/upstream-snapshots/` 下最新日期目录的快照（预存校对基准）。
3. 相关既有实现速查：
   - 比率引擎：`app/Services/CodingPlanRatioService.php`（exact > prefix > 默认；per_request / per_1k_tokens / per_token_parts 三段率；time_discounts 时段折扣，北京时间口径固定 `Asia/Shanghai`）
   - 官方模板目录：`app/Services/CodingPlanCatalog.php`（厂商/档位/比率只读模板 + apply）
   - 6 小时校对命令：`app/Console/Commands/VerifyCodingPlanRatios.php`（stale 检测 + `pricing_source_url` JSON 源 diff：new / changed(split_rates, time_discounts) / missing；**只记录不自动改价**，管理端「官方同步」页应用/忽略）
   - 调度：`routes/console.php`（`pease:sync-coding-plan-official` + `pease:verify-coding-plan-ratios`，均 everySixHours，先 sync 后 verify）
   - 官方源同步（2026-09-12 新增）：`app/Services/CodingPlanOfficialSourceService.php`（15 家厂商源注册表 + 代理抓取 + 快照保留 10 份 + 复用 diff/忽略标记/流水写入）+ `app/Services/CodingPlanParsers/*`（deepseek 转置表 / openai Markdown 已就绪，其余待补）+ `coding-plan:sync-official` 命令
   - 模型：`CodingPlanVendor`（billing_mode、plan_kind=1 Coding Plan/2 Token Plan、unit_exchange_rate、docs_url、pricing_source_url）、`CodingPlanVendorTier`（官方档位）、`CodingPlanModelRatio`（比率 + time_discounts）、`CodingPlanRatioCheck`（校对流水）、`CodingPlanAccount`（账号池）
   - 回归自检：`php artisan coding-plan:test-time-discounts`（28 项断言）+ `php tests/coding-plan-parser-fixture.php`（解析器 21 项断言）
   - 数据表迁移序号：000002 厂商预置 → 000003~000009 官方价目/时段折扣（详见 operations-guide.md §8）
4. 任务完成定义：代码 + 迁移/预置数据 + 测试（能跑则跑 `php artisan` 验证）+ 文档（operations-guide.md 对应小节）+ 本文件勾选。
5. **双仓库同步（2026-09-12 起）**：开源仓库两份——GitHub `origin`（`git@github.com:PeaseAPI/PeaseAPI.git`，`git push` 默认推送）+ Gitee `gitee`（`git@gitee.com:oyu/PeaseAPI.git`，SSH 公钥已配，页面 https://gitee.com/oyu/PeaseAPI）。**每次 commit 后必须双推**：`git push` + `git push gitee main`，确保两边 HEAD 一致；本地 main 跟踪 origin/main，Gitee 走显式 remote 推送。

---

## 1. 背景与目标（用户原始需求拆解）

| # | 需求 | 对应任务 |
|---|------|----------|
| R1 | 覆盖所有已支持 AI 厂商的套餐：Token Plan / Coding Plan，个人版/团队版/企业版 | 已有 `CodingPlanVendor`+`Tier` 骨架；P3 补数据、P3-6 补新厂商 |
| R2 | 折扣率、积分制、按次/按 token 均要支持 | 已有三 cost_mode + unit_exchange_rate 两级折算 ✅ |
| R3 | 分时段折扣（如 DeepSeek 错峰），细化到输入/输出/缓存命中折算积分 | 已有 time_discounts + 三段率（迁移 000009）✅，P3 补录剩余厂商 |
| R4 | 找不到网页的厂商自行检索补全（OpenAI 等，必要时走代理 `http://127.0.0.1:7890`） | P0-2 快照 + P3-6 预置 |
| R5 | 每家找到**确定**的官方页面，每 6 小时抓取 + 与预存校对一次 | **P1 官方源适配器（核心工程）** |
| R6 | 校对不只折扣积分，还要**模型新上架/下架** | P1-6 模型目录源 + check kind=model_catalog |
| R7 | 活动结束时间提醒客户 | P2 活动表 + 到期提醒 + 介绍页倒计时 |
| R8 | 拆分细节形成任务，重启后可恢复 | 本文件 ✅ |
| R9 | **多货币计费**：厂商标注官方计价币种（OpenAI/Google=USD，Google 另有 HKD 区），平台配置汇率，客户可选结算货币 | **P7 多货币与结算体系** |
| R10 | **厂商→模型上架流**：选定厂商后列出其可选模型，勾选是否提供 | **P8 厂商模型上架流** |
| R11 | **成本感知路由**：同模型多供应商报价不同（联通 Coding Plan GLM-5.1 最便宜）→ 低成本源优先调度，耗尽才 failover 到贵的 | **P9 成本感知路由** |
| R12 | **配额恢复感知**：上游「时间窗用量达上限」时记录恢复时间点，期间调度跳过，到点自动切回 | **P9 配额恢复感知** |

---

## 2. 数据流架构（目标态）

```
官方页面/接口（每家厂商确定 URL，含需代理的国际源）
        │  CodingPlanOfficialSourceService（内置 source 注册表：url/format/解析器/代理标记）
        ▼
标准化条目 {model, match_type, cost_mode, unit_cost|input/cached/output_rate, time_discounts}
        │  快照预存 storage/app/private/coding-plan-snapshots/{vendor}/{Y-m-d-Hi}.json（Laravel 11 local disk root=storage/app/private；保留最近 10 份）
        ▼
与库内 coding_plan_model_ratios / 上一次快照 diff
        ▼
coding_plan_ratio_checks（new / changed / missing / time_discounts / model_catalog）
        ▼
管理端「官方同步」页人工确认（应用 / 忽略）——绝不自动改价
        ▼
活动类变更同步写 coding_plan_promotions → 到期前 7 天提醒（公告/邮件/介绍页倒计时）
```

---

## 3. 当前执行指针

> **下一步**：**QA 全量功能自检（2026-09-12 第十九轮，R13）**——按「### QA 全量功能自检」清单逐项检查（工程静态 → 自检命令 → 服务器部署态 → 网站 E2E → relay 真实调用火山 ark），所有发现记入该节问题表并逐条修复；每轮：本地验证 → git commit+push → 服务器 `git pull` → migrate/缓存清理 → 重跑检测。P0~P8 已完成；P9（成本感知路由）与 P1-9/P1-5b/P4-3/P4-4/P5-x 待 QA 通过后继续。新需求 R9-R12 → P7/P8/P9（见任务清单末尾）。

---

## 4. 任务清单

### P0 需求梳理、检索与快照预存（2026-09-12 本轮完成）
- [x] P0-1 现状盘点：确认已有比率引擎/校对命令/调度/预置数据（见 §0）
- [x] P0-2 首轮官方抓取并预存快照：`docs/upstream-snapshots/2026-09-12/`（sources.md 数据源清单 + pricing-data.md 抓取数据；OpenAI 走代理 + `.md` 后缀技巧；联通/火山/移动为 SPA，方案记录在快照内）
- [x] P0-3 建立本任务账本

### P1 官方源自动抓取适配器（核心工程，下一步从这里开始）
- [x] P1-1 新建 `app/Services/CodingPlanOfficialSourceService.php`：source 注册表（vendor → {url, format(json|markdown|html), parser, proxy:bool, model_catalog_url?}），可被 `coding_plan_vendors.pricing_source_url` 覆盖（自管源优先）✅ 2026-09-12（15 家注册表齐备；解析器未就绪的源仅存原始快照 `.raw.txt`）
- [ ] P1-2 逐厂商解析器（输入=HTTP 响应体，输出=标准化条目数组，格式见 P1-3）：
  - [x] deepseek：`https://api-docs.deepseek.com/zh-cn/quick_start/pricing/`（**真实结构=转置表**：模型为列、指标为行、rowspan 分段、单位在格内如「0.02元」；纵向表做兜底；空闲/高峰 → time_discounts 窗口与迁移 000009 零漂移）✅ 2026-09-12 端到端「无变更」
  - [x] openai：`https://platform.openai.com/docs/pricing.md`（**需代理**；Markdown 表；short/long context 双列 + Batch 半价表 + cache writes 列）✅ 2026-09-12 全链路：解析 67 模型（长上下文价忽略、裸缩写补全、Batch 首表优先、无缓存价=输入价保守）→ diff「新增 67」待人工确认
  - [x] google：`https://ai.google.dev/gemini-api/docs/pricing`（**需代理**；SSR 中文机翻 HTML；模型名只在锚点 id，表在 standard/batch/flex/priority 子标题下）✅ 2026-09-12 全链路：解析 28 模型（只取 standard 层；双价取「起为」恢复价=2027-01-01 长期价，促销价归 P2；分档长上下文取首档；「/小时（存储价格）」「每张图片」换算价、模态列表「/ 图片」精确区分）→ diff「新增 28」待人工确认
  - [x] anthropic：`https://docs.anthropic.com/en/docs/about-claude/pricing`（**需代理**；Next.js SSR，表格由 div+CSS 渲染无 `<table>`，按 `<tr>` 平铺扫描）✅ 2026-09-12 全链路：解析 17 模型（显示名「Claude Opus 4.6」→ claude-opus-4.6；「Model」表头行重置列定位；Batch 半价表/CCU 说明表/1M 长上下文合并名行跳过，同模型首条为准）→ diff「新增 17」待人工确认
  - [x] xai：`https://docs.x.ai/developers/models.md`（**.md 直取**；原 /docs/models 308 → /developers/models；grok-4.6 $2/$0.5/$6；长上下文分档取 < 200k 首档、≥200k 条件价跳过；Imagine/Voice 按次计价表跳过）→ 真抓 7 模型「新增 7」✅2026-09-12
  - [x] zhipu：`https://docs.bigmodel.cn/cn/coding-plan/overview.md`（Mintlify `.md` 直取；页面=套餐积分配额表，**无模型按量价**）✅ 2026-09-12：`catalog_from_pricing` 复用同响应体产 `model_catalog`（GLM‑5.3 等 6 模型，U+2011 非断连字符归一化）→ 目录 diff「新增 4」待人工确认；按量三率已预置迁移 000009
  - [x] aliyun：源重定位 ✅2026-09-12——help.aliyun.com 旧页 404 → `docs.bailian.console.aliyun.com/llms.txt`（126KB 索引）+ `llms-full.txt`（5.7MB 全站拼接）可抓；**单页 .md 直取被 WAF 拦**（要求动态 `X-Request-Context` 头）；内容=Token Plan 个人/团队**套餐档位**（Lite 39/Standard 139/Pro 499 元 + 用量包，CNY，无按量 token 价）→ 注册表改指 llms-full.txt（parser=null 仅存快照），档位解析归 P2-1、币种归 P7-1
  - [x] moonshot：`https://platform.kimi.ai/docs/pricing/chat.md`（**需代理**）✅ 2026-09-12 全链路：platform.moonshot.cn 301 → platform.kimi.com（Kimi 开放平台）；llms.txt 索引 + .md 直取，表格为 JSX `<DocTable rows={[...]}>`；**注册国际站 kimi.ai（$），中文站 kimi.com 为 ¥ 不配**；列序=命中价在前（与 DeepSeek 相反）；batch/tools 独立页不配 → 真抓 4 模型，**与库内预置零 diff（交叉验证通过）**
  - [x] tencent：`https://cloud.tencent.com/document/product/1823/130060`（Slate SSR；套餐积分配额页无按量价 → `catalog_from_pricing`；Model ID 表逐变体拆分——`data-slate-string` 每 ul 一个 ID，点/横线双风格并存；「整格全 ID」规则滤掉概览/工具生态格）→ 目录新增 16 ✅2026-09-12
  - [x] siliconflow：`https://siliconflow.cn/pricing`（SSR 直出 ~225KB）✅ 2026-09-12（九）——`pricing-row-{text|image|audio|video}-*` 行 + `<a title="org/model">`（48 款全模态目录，`Pro/` 前缀=加速标记剥除）→ **SiliconflowParser** 真抓 48 模型目录「新增 48」；按量价 ¥/M tokens（CNY，双时段「9点～18点」价组）→ 折算比率归 P7-2/P7-3（时段价可映射 time_discounts）；currency 字段（P7-1 迁移 000010）已落地解除阻塞
  - [x] volcengine：`https://docs.volcengine.com/api/doc/getDocDetail?DocumentID=1544106&lang=zh` ✅ 2026-09-12（六）——doccenter garfish SPA 无内嵌数据、llms.txt 301 → **从前端 bundle 挖出 XHR API**（`grep '/api/doc/'` main.js，旧 doc 1099320 已 301 → 1544106 model-pricing）；响应 `Result.Content` = **Quill delta JSON 字符串**（zone：Z 正文/R 行/C cell 引用/x* cell 文本）→ 逐 zone 正则提 `doubao-*` → 真抓 38 模型目录「新增 38」；按量价=元/百万 token（CNY）→ 三率归 P7-1
  - [x] unicom：`support.cucloud.cn/document/127/591/2357.html?...arcid=7015/7080`（✅ 2026-09-12 探测反转：**无 XHR**——DedeCMS 页面 JS 内嵌 `totalList = [...];` 全站文档树（16.5MB，113 篇，`documentEntityList[].content`=富文本 HTML，id=URL 的 arcid）→ **UnicomParser**：深度状态机定位 JSON 结束（`];` 误定位——JSON 后还有 27KB 其他 JS）→ BFS 抽 7015/7080 两篇「支持模型」列（块级 `</p><p>` 先换行防粘连）→ 顿号/空白拆分 + 「别名：说明」取冒号前段 → 真抓 **11 模型目录「新增 10」**（aisp-auto-route 命中库内预置）；套餐档位 CNY 归 P2-1，credits 折算综合单价仅参考归 P7-1）
  - [ ] cmcc：`https://ecloud.10086.cn/op-help-center/doc/article/98322` 与 `outline/108724`（探测结论：React SPA `cloud-cms-service-web` 纯壳 1KB → 文档内容走 CMS XHR，API 未定位，待挖 app.js）
  - [x] minimax：`https://platform.minimax.io/docs/guides/pricing-paygo.md`（**需代理**）✅ 2026-09-12 全链路：minimaxi.com/minimax.io 均 llms.txt + .md 直取；**注册国际站 minimax.io（$），国内 minimaxi.com 为 ¥ 不配**；Priority Tab（1.5x 条件价）与 Legacy Accordion（M2.5/M2.1）整段剥离；M3 ≤512k/>512k 分档取首档；划线促销「~~$0.60~~ $0.30」Permanent 50% off 取实价 → 真抓 3 模型「新增 3」待人工确认
  - [x] baidu：`https://cloud.baidu.com/doc/qianfan/s/wmh4sv6ya` ✅ 2026-09-12（六）——旧 doc hlpl7xe2f 已 302 → 从 index 定位新页（415KB **全 SSR**，217 行价格表）；「模型名称|版本名称|服务内容|子项|在线推理|批量推理|单位」表，「版本名称」列=API 调用 id 但**连排无分隔**（`ERNIE-5.1ERNIE-5.1-Speed-Preview`）→ 按 `(?:ernie|bce)-` 前瞻切分 + modelName 白名单；量包表无「版本名称」表头自动跳过 → 真抓 31 模型目录（ERNIE + 托管 deepseek/glm/kimi/qwen）「新增 30」；按量价=元/千 tokens（CNY，单位天然 /千届时无需 ÷1000）→ 三率归 P7-1
- [x] P1-3 统一标准化格式（与 `VerifyCodingPlanRatios::fetchSource` JSON 约定一致：`models[]{model,match_type,cost_mode,unit_cost,input_rate,cached_rate,output_rate,time_discounts}`）✅ 实现于 `CodingPlanOfficialSourceService::normalizeStructuredEntries()`；解析器同约定输出（单位统一=币种/1k tokens，官方每百万标价 ÷1000）
- [x] P1-4 快照预存：`storage/app/private/coding-plan-snapshots/{vendor}/{Y-m-d-Hi}.json` + 清理策略（保留 10 份，`.raw.txt` 随 JSON 连带清理）；抓取失败沿用上次快照并在 check 记 source_failed ✅ 2026-09-12
- [x] P1-5 diff 与预存基准对比（把现有 diff 逻辑抽成可复用方法），结果写 `coding_plan_ratio_checks` ✅ 2026-09-12（`fetchStructuredSource()`/`diffEntries()`/`diffCatalogModels()`/`markIgnoredChanges()`/`recordCheck()`；diffEntries 兼容 keyed map 与 list 输入，数值 diff 仅对启用行、存在性比对含停用行）
  - [ ] P1-5b `VerifyCodingPlanRatios::diffPricingSource` 切换到复用方法（低优先重构，现有实现工作正常）
- [ ] P1-6 模型上下架检测：各厂商「模型列表」源单独解析 → `kind=model_catalog`（new=上架 / missing=下架；含 OpenAI `/docs/models.md`、xai Retirement、腾讯 GLM-5/5.1 2026-10-09 下线等）【diffCatalogModels + openai 目录源、zhipu catalog_from_pricing（目录与定价同一响应体，注册表布尔开关）已接，其余厂商目录源待接】
- [x] P1-7 新命令 `coding-plan:sync-official {--vendor=} {--snapshot-only}`：抓取→快照→diff→check 串起来；调度每 6 小时（先 sync 后 verify）；保留手动 `pricing_source_url` 自管源 ✅ 2026-09-12（`--vendor` 为合并而非过滤，与 verify 同口径；快照含 proxy_used/parser/entries/catalog 元数据）
- [x] P1-8 代理支持：`PEASE_API_HTTP_PROXY` env（默认空；本机开发 `http://127.0.0.1:7890`），仅对 proxy:true 的源使用；写进 settings-reference.md ✅ 2026-09-12
- [x] P1-9 健康告警：同一源连续 ≥2 次抓取失败 → check 流水标记 + 管理端同步页红点 ✅ 2026-09-12（`CodingPlanRatioCheck::SOURCE_FAILED_ALERT=3` 常量零迁移；`CodingPlanOfficialSourceService::resolveSourceStatus()` 统一升级判定（sync-official 与 verify-ratios 双写入点共用，查同 vendor 最近流水前条为失败态则升级）+ `sourceHealth()` 按 vendor 推导连续失败数/最近成功时间/alerting；`/admin/coding-plan` 页首「官方源同步健康」卡片（告警态红边框+红 badge+table-danger 行，显示连续失败次数/最近成功/最后检查）+ 侧边栏「Coding Plan 池」菜单红点 badge（Cache 5 分钟缓存，异常兜底 0 不阻塞渲染）；命令输出区分 `source_failed`/`source_failed_alert（连续失败告警）`）
- [x] P1-10 解析器单元测试：以 `docs/upstream-snapshots/2026-09-12/` 存档为 fixture，断言解析数值 ✅ `php tests/coding-plan-parser-fixture.php`（42 项断言：deepseek 转置/纵向、openai 长上下文/Batch/裸缩写、google 双价恢复价/分档/存储与图价噪声、anthropic 显示名转 id/Batch 去重/CCU、zhipu U+2011 归一化等容错）

### P2 活动与到期提醒（R7）
- [x] P2-1 迁移新表 `coding_plan_promotions`：vendor, kind(=discount/free/price_change/model_retirement), title, description, discount, starts_at, ends_at(可空=官方未公布), source_url, status, remind_days(默认7), sort, remark ✅ 2026-09-12（十三）——附管理 API promotions GET/POST/DELETE 三路由（对齐 rates 风格：列表带 state 推导 scheduled/ongoing/expired + remaining_seconds 倒计时，ends_at=0/null=官方未公布长期有效；POST upsert 传 id 即更新）；模型 `CodingPlanPromotion`（KIND_/STATUS_ 常量 + displayState/remainingSeconds）；SCNet 套餐档位同轮落地：`CodingPlanCatalog::templates()` 新增 `scnet` 模板（厂商行 status=0 默认停用 + 基础/标准/高级/旗舰 30/110/265/764 元 CNY 活动价，原价 50/185/440/1274 注 price_note，月度 Credits 6 万~180 万两端已录、中间档待核对，倍率示例 glm-5.3=2.29/kimi-k3=4.12 status=0 参考）+ 迁移 000014 `CodingPlanCatalog::apply('scnet')`（000006 同构，币种补 CNY）
- [x] P2-2 迁移预置已知活动（均注来源 URL）✅ 2026-09-12（十四）——迁移 000015 预置 9 条：智谱夜间畅用（每日 23:00–09:00，ZCode 端 GLM-5.3-Flash 畅用/其他额度翻倍）、阿里云个人版限时价（39/139/499 原价 60/180/600，kind=price_change）、阿里云夜间五折（22:00–08:00，qwen3.8-max/deepseek-v4-pro-0813/deepseek-v4-flash-0731，discount=0.5）、火山方舟 Auto 系数 0.5（至 2026-11-08）、腾讯 GLM-5/5.1 下线（2026-10-09，model_retirement）、Google 促销价（至 2026-12-31）、OpenAI gpt-5.6-sol（至少至 2026-11-21）、xAI Imagine 退役（2026-11-02，model_retirement）、移动首订特价（7.9/39.9 元至 2026-12-31，Catalog cmcc 注释背书）；每日循环型 ends_at=null（3 条），有截止按北京时间 23:59:59 落 Unix 秒；幂等 vendor+title firstOrCreate + remark「官方活动（预置 2026-09-12）（P2-2）」标记 down 可清：
  - 智谱「夜间畅用」每日 23:00–次日 09:00：ZCode 端 GLM-5.3-Flash 畅用、其他 Agent 额度翻倍（docs.bigmodel.cn/cn/coding-plan/overview）
  - 阿里云 Token Plan 个人版限时价 Lite 39 / Standard 139 / Pro 499（原价 60/180/600，截止未公布 → ends_at=null + note）
  - 阿里云夜间五折 22:00–次日 08:00：qwen3.8-max / deepseek-v4-pro-0813 / deepseek-v4-flash-0731
  - 火山方舟 auto 活动系数 0.5 **至 2026-11-08**
  - 腾讯 TokenHub：GLM-5 / GLM-5.1 **2026-10-09 下线**（kind=model_retirement）
  - Google Gemini 促销价 **至 2026-12-31**（2027-01-01 恢复原价）
  - OpenAI gpt-5.6-sol 促销价 **至少至 2026-11-21**
  - xAI Imagine 图像相关 **2026-11-02 退役**（kind=model_retirement）
- [x] P2-3 提醒链路：调度每日 09:00（新命令 `coding-plan:remind-promotions`）扫描 remind_days 内到期活动 → ①站内公告 ②邮件通知订阅对应厂商套餐的用户 ③介绍页倒计时徽标（前端归 P7-5）；到期后自动置过期并记流水防重发 ✅ 2026-09-12（十五）——①迁移 000016 `coding_plan_promotion_reminders` 流水表（promotion_id+kind+user_id 唯一防重发；user_id=0=站内公告全局、>0=用户邮件；kind=ending/expired）。②模型 `CodingPlanPromotionReminder` + `CodingPlanPromotion::STATUS_EXPIRED=2`（到期自动置 2，前台 enabled 口径自然退出、管理员 all=1 可见）。③Mailable `PromotionReminderMail`（ShouldQueue 队列发送）+ `emails/promotion-reminder` blade。④命令 `coding-plan:remind-promotions`（--dry-run 全程零写入）：临期（remaining ≤ remind_days×86400）→ Option「Notice」追加活动段落（全局一次）+ 订阅对应厂商套餐的有效用户（subscriptions ⋈ subscription_plans.coding_vendor，status=1 且 period_end≥now）各一封队列邮件；已过期 → status 置 2 + kind=expired 流水留痕；防重发全靠流水唯一约束（insertOrIgnore 冲突跳过，删流水可强制重发）。⑤调度 routes/console.php 注册（Schema::hasTable('coding_plan_promotions') 守卫，onOneServer dailyAt 09:00）。自检 14 项（dry-run 零写入/公告段落/流水防重发双跑/自动置过期/退出前台/无订阅 0 封/订阅用户 2 封含补发/唯一索引/回滚零残留）
- [x] P2-4 公开 API `GET /api/coding_plan/promotions`（仅 status=1 且未过期）+ 管理端维护页 + i18n ✅ 2026-09-12（十七）——公开端点定名 `GET /api/coding_plan/public_promotions`（管理端 `GET /coding_plan/promotions` 与公开版同 URI 会互相覆盖路由，故独立命名；自检验证无 token 时管理端 401 / 公开端 200 双轨隔离）；口径 `enabled()` 且 `displayState≠expired`（scheduled 预告 + ongoing 进行中展示），字段白名单（id/vendor/kind/title/description/discount/starts_at/ends_at/source_url/state/remaining_seconds/sort，status/remind_days/remark 不外露）；管理端维护页（十六）已完成。i18n 移交 P4-3 统一改造。自检 22 项（端点口径/三种状态可见性/status=0 剔除/倒计时/回滚零残留/路由隔离）

### P3 数据补全与新增厂商预置（R1/R3/R4）
- [ ] P3-1 阿里云夜间五折三行比率（qwen3.8-max 等；官方三率未公布，先建行 status=0 + time_discounts 22:00→08:00 跨零点窗口模板）
- [ ] P3-2 腾讯旧逻辑模型逐档价（deepseek-v4-*、glm-5/5.1/5.2、minimax-m2.7）
- [ ] P3-3 百度积分制逐模型系数（官方「即将支持」后补录）
- [ ] P3-4 移动个人版视觉/视频模型豆率（按需）
- [ ] P3-5 新厂商预置：`siliconflow`（时段价样例见快照 §8）、`minimax`（M3/M2.7）、Kimi K3 官方价核对更新
- [ ] P3-6 国际按量厂商预置（plan_kind=2 Token Plan，单位=美元/百万 tokens）：openai（快照全表）、google（促销价+恢复价双列备注）、xai（grok-4.6）、anthropic（拿到源后）
- [ ] P3-7 把 2026-09-12 快照与现库不一致处核对后更新进 `CodingPlanCatalog` 模板与新迁移（幂等 upsert，不改已启用人工行）：阿里云个人版限时价 39/139/499 + 7 天限额 2500/10000/40000 + 用量包 100 元/2 万 Credits 等（diff 明细见快照文末）

### P4 前端与文档
- [x] P4-1 介绍页 `/coding-plan`：活动倒计时徽标、厂商最后核对时间/源状态、模型上下架公告位 ✅ 2026-09-12（十七）——公开路由 `routes/coding-plan/index.tsx` + `features/coding-plan-introduce/index.tsx`（管理端原 `_authenticated/coding-plan` 与公开页同 URL 冲突致 routeTree 停摆，挪至 `_authenticated/coding-plan-manage` + 侧边栏同步）。页面=①官方活动区（消费 public_promotions：vendor/kind 徽标、倒计时徽标三档色（≤72h 红/≤7 天黄/其余灰）、截止时间、官方公告外链，kind 覆盖 discount/free/price_change/model_retirement 即上下架公告位）②厂商卡片按 plan_kind 分组（订阅制/按量）：billing/currency 徽标、最近核对时间 + source_status 徽标（一致/源异常/未核对）+ stale/change 计数、官方文档外链③档位表（官方原币价 + 折算 CNY 双列 + 周期/额度/备注）④模型抵扣率表（cost_mode/单位成本/三段率/时段折扣/超期徽标，max-h 滚动）。typecheck ✓ oxlint 0 error ✓ build ✓；i18n 留 P4-3（当前与管理端一致中文硬编码）
- [ ] P4-2 管理端「官方同步」页：抓取历史（快照列表/失败原因）、源健康状态、promotions 维护入口
- [ ] P4-3 i18n：`public/src/i18n/locales/*.json` 补齐新增文案（zh/en 最少，其余跟随）
- [ ] P4-4 文档：operations-guide.md §4/§8、usage-guide.md、settings-reference.md 增补 sync-official / promotions / 代理配置

### P5 测试与验收
- [ ] P5-1 适配器单测（fixture=快照存档）
- [ ] P5-2 全链路手测：`php artisan coding-plan:sync-official` → checks 有新变更 → 管理端应用 → 比率生效；`coding-plan:test-time-discounts` 仍 28/28 通过
- [ ] P5-3 迁移全量回放：`php artisan migrate:fresh` 后 seeder/预置无报错（宝塔环境注意 `unsignedDecimal` 历史坑）
- [ ] P5-4 提醒链路手测：造一条 ends_at=明天的活动 → 命令跑一次 → 公告+邮件生成且次日不重发

### P6 持续运营约定
- [ ] P6-1 每 6 小时自动抓取校对（调度已定）；每周人工抽查 2 家源
- [ ] P6-2 官方公布新套餐/活动时：先更新快照 → 再进 Catalog/迁移 → 勾选对应任务

### P7 多货币与结算体系（R9，2026-09-12 新增）
- [x] P7-1 迁移 000010：`coding_plan_vendors` 加 `currency`（官方计价币种，默认 CNY；openai/google/anthropic 预置 USD——xai 库内尚无厂商行、接入时自落 USD，google HKD 区备注见迁移注释），`coding_plan_vendor_tiers` 加 `currency`（档位标价币种，可空=NULL 继承厂商）✅ 2026-09-12（九）
- [x] P7-2 汇率配置：迁移 000011 新表 `currency_rates`（code PK、rate 相对基准 CNY=1 单位折人民币数、source=manual/api、updated_at Unix 秒）+ 管理端 API CRUD（GET/POST/DELETE `/coding_plan/rates`，upsert 语义）；`CurrencyExchangeService::rate()`（P7-3 服务类先行落读取侧）：表值优先 → USD 无行回落 Option `UsdExchangeRate`（实测回落生效值 7.3）→ CNY 恒 1 → 其余无行 null；vendor/tier CRUD validate 补 `currency` 字段（P7-1 字段后端可管理；前端维护页归 P7-5）✅ 2026-09-12（十）
- [x] P7-3 `CurrencyExchangeService`：`convert(amount, from, to)`（以基准 CNY 中转双向折算，任一端不可折算 → null 调用方拒绝）+ `snapshotFor()`（汇率快照 {base,from,to,rates,taken_at}，纯基准币种返回 null 不留痕）+ `vendorOfficialCurrency()`（memo 查 vendors.currency）；计费落库挂点：`CodingPlanPoolService::recordUsage` 自动把厂商官方币种非 CNY 时的 fx 快照写进用量 log `meta.fx`（调用方可显式覆盖，汇率随时间变化可审计）；订单侧暂无非基准币种折算发生点，预留 meta.fx 同构 ✅ 2026-09-12（十一）——自检 `php artisan coding-plan:test-currency` 21 项断言（回落/互逆/快照/vendor 币种/recordUsage 集成，事务内零残留）；CodingPlanVendor fillable 补 `currency`
- [x] P7-4 客户结算货币：迁移 000012 `users.settlement_currency`（string(8) nullable after stripe_customer，NULL=跟随平台基准 CNY，幂等守卫）；`User::settlementCurrency()`（null/空 → CNY，存储归一大写）；`CurrencyExchangeService::convertQuota(quota, currency)`（quota ÷ QuotaPerUnit=美元单位 → convert() 折偏好币种，随附 fx 快照={base,from,to,rates,taken_at} 结算口径=交易时刻汇率快照；无偏好/基准/不可折算 → null 调用方按平台默认展示）；`GET /user/self` 响应带 `settlement_currency` + `settlement` 换算块（报价/账单/余额展示层换算的数据源，P7-5 前端消费）；`PUT /user/self` 可改偏好（空值=清除，非空须 isSupported 否则 422）✅ 2026-09-12（十二）——自检 test-currency 增【F】段（30 项断言累计）
- [x] P7-5 前端：vendors-tab 加币种选择 + 汇率维护页 ✅ 2026-09-12（十六）（十七收尾全量完成）——vendors-tab 表单/表格加 `currency`（7 币种 select + 币种列）；新增「汇率」tab（rates CRUD 表格 + hints 生效值提示：CNY 基准/USD Option 兜底）、「厂商活动」tab（promotions 全字段 CRUD + state 徽标 + 倒计时 + 含停用过期开关，复用 vendors 列表供厂商代码 datalist）；typecheck ✓ oxlint 0 warning ✓ build ✓。（十七）后端 `publicOffers()` tiers 增 `currency` + `price_cny` 双列（vendor.currency 经 `vendorOfficialCurrency()` 统一口径，不可折算→null；storeRate/destroyRate 后失效 offers 缓存）；介绍页（P4-1）落地双列价与活动倒计时徽标。全部 P7-5 项完成

### P8 厂商→模型上架流（R10，2026-09-12 新增）
- [x] P8-1 模型清单 API：`GET /api/coding_plan/vendors/{code}/models`（聚合 Catalog 模板 + 比率表 + 官方目录快照，标注：已提供(status=1)/已停用/官方新增/官方下架）✅ 2026-09-12（十八）
- [x] P8-2 上架流页面：管理端选厂商 → 模型清单勾选 → 批量启用/停用比率行（status）→ 联动渠道 ability/models 映射（复用 SyncChannelCache）✅ 2026-09-12（十八）
- [x] P8-3 官方同步的 `model_catalog` new/missing 变更在清单页高亮 + 一键应用/忽略（承接 P1-6 check 闭环）✅ 2026-09-12（十八）

### P9 成本感知路由与配额恢复感知（R11/R12，2026-09-12 新增）
- [x] P9-1 成本排序器：同模型多渠道/多厂商候选，按「每 1k tokens 平台成本」（三段率 × 时段折扣 × unit_exchange_rate × 币种汇率）升序生成动态优先级；模式开关 `ModelRouteStrategy`（cost_first | static，默认 static 保护现网行为）✅ 2026-09-12（二十三）——新增 `CostRouteService`：渠道→`coding_plan_accounts.channel_id` 关联账号（与 RelayInfo::applyCodingPlanAccount 同口径取首个非停用）→vendor→`CodingPlanRatioService::resolve`（exact>prefix>全局默认，复用 300s 缓存）→探针单位（per_token_parts=0.75×input+0.25×output，750/250 业界典型配比、缓存命中不假设=保守、三段全 0 回退 unit_cost 对齐 calcUsage 防资损；per_1k_tokens=unit_cost；per_request=unit_cost，探针语义=1k tokens 预算≈一次请求）→×Asia/Shanghai 时段折扣×effectiveExchangeRate（账号级>vendor>1，与扣费同一实现）=cost_per_1k；币种因子已隐含于 unit_exchange_rate 配置语义故不再乘市场汇率（防双重折算），另输出 cny_reference=探针×官方币种→CNY 市场汇率仅供 P9-4 观测；无账号关联的普通 API 渠道 cost=null 排序垫底按 priority 兜底；同成本 priority desc→id asc 稳定序。`ModelRouteSetting` 策略开关（Option `ModelRouteStrategy`：static 默认/cost_first，非法值回退）；`ChannelSelectService::pickChannel` 接 cost_first 分支（static 路径 SQL 原样零改动）+ 新增 `candidateChannels()`（abilities 匹配+default 回退+status=1，供 P9-2 failover 复用）。自检 `coding-plan:test-cost-routing` 24 断言（A 策略开关/B static 零变化+成本排序+三口径数值/C 时段折扣反超/D model_mapping 后比率解析/E cny_reference 汇率/F 剔除垫底兜底/G 零残留含缓存键）。踩坑：①flushCache 只清目标实例进程内 memo，事务内造数变更需重建服务实例；②flushCache 全量分支按 vendors∪ratios 表枚举 vendor——回滚后自检 vendor 的 `coding_plan_ratios:*` 键漏清会跨轮污染（finally 显式 forget + G 段断言防回归）。回归：cost-routing 24/24、time-discounts 28/28、currency 30/30、parser fixture 全过、pint PASS
- [x] P9-2 跨源 failover：首选源（如联通 Coding Plan 账号池）配额耗尽（`pickAccount` 无可用账号 / 全员 STATUS_EXHAUSTED）→ 自动切换次选源渠道；路由决策写用量日志（reason=cost_failover，记录落选候选与成本）✅ 2026-09-12（二十四）——`RelayHandler::establishUpstreamChannel` 替换 handle/handleStream 中直接调用（正常路径与 `applyCodingPlanAccount` 零差异，仅「account pool exhausted」RuntimeException 触发 failover，订阅校验失败等原样上抛不参与）+ `failoverChannel`：候选=`ChannelSelectService::candidateChannels`（P9-1 预留复用点）排除已败渠道、每候选 setChannel→`setModel` 重映射（各渠道 model_mapping 可能不同）→`selectAdapter`（换 type 重选适配器）→`applyCodingPlanAccount`，RuntimeException 落选记 attempted 继续；序=cost_first 成本升序（`CostRouteService::sortChannelsByCost` 明细带 vendor/cost_per_1k）/static priority desc+id asc，上限 5 个防请求拖死；普通 API 渠道（无池）天然通过=跨源兜底归宿；全败 throw 原始异常走既有 catch(Throwable) 退款路径。`RelayInfo::routeDecision`（reason/strategy/failed/final/attempted[]/decided_at）经 recordCodingPlanUsage 快照并入 `coding_plan_usage_logs.meta.route` 落库（仅 failover 发生时非空，正常路径零开销）；顺手修两个真 bug：`setModel` 重映射未命中重置 isModelMapped（换渠道后映射状态残留）、`applyCodingPlanAccount` 非池分支清残留 codingVendor/codingPlanAccount（池耗尽抛异常前已设 vendor）。踩坑：RelayHandler 原 use 无 `RuntimeException`——catch(RuntimeException) 被解析为不存在命名空间类永不命中（php -l 不报错，自检 B 段抓出）。自检 `coding-plan:test-cost-failover` 24 断言三轮幂等（A 池健康零变化/B cost_first 切换+重映射/C 多候选落选成本留痕/D meta.route 落库/E 全败抛原始/F static 序/G 零残留含缓存键）。回归：cost-routing 24/24、time-discounts 28/28、currency 30/30、parser fixture 全过、adapters-verify ALL PASS、pint PASS
- [x] P9-3 恢复回归：上游「已达时间窗上限」（quota_exceeded/429）→ 账号池已有 `reset_*_at` 自动恢复（`CodingPlanPoolService::resetExpiredWindows`），补渠道级 `cooldown_until` + 解析上游 Retry-After/重置文案；恢复到点调度自动切回低成本源；无窗口信息时用保守默认（如 5h 窗口起点）✅ 2026-09-12（二十五）——迁移 000017 双字段：`coding_plan_accounts.cooldown_until`（账号级精准恢复点）+ `channels.cooldown_until`（渠道级，池全不可用=最早恢复点/任一可用=0）。`RelayInfo::resolveQuotaCooldown()` 四级解析：Retry-After 头（秒数/HTTP 日期，大小写不敏感）→ JSON reset_time/resetTime/reset_at/resets_at（秒级；>1e12 毫秒 /1000；顶层或 error 内）→ 中英文案（Resets at / try again at / 重置于 / 恢复于 + 日期或 HH:mm，仅时间落今天已过视为次日）→ null；handle 失败路径 quota_exceeded 时传入 `recordCodingPlanUsage(,,?cooldownUntil)` → `PoolService::recordUsage` 写账号冷却，**派生序=上游解析值 > 窗口恢复点（reset_5h/weekly/monthly 最小未来点）> 保守默认 now+5h**——修复 quota_*=0（未启用周期限制）账号被 429 打标耗尽后 reset_*_at 恒 0 → `resetExpiredWindows` 永不处理的「永耗尽」缺口。新方法 `recoverExpiredCooldowns`（pickAccount 前置：cooldown 到点 EXHAUSTED→ENABLED+清 0）+ `refreshChannelCooldown`（池可用→清渠道冷却；全不可用→最早恢复点/5h；正常耗尽与 quota_exceeded 与窗口恢复后均同步）。`ChannelSelectService::candidateChannels` 过滤冷却渠道 → cost_first 恢复到点自动切回低成本源 + failover 候选天然跳过；**static pickChannel SQL 零改动**（P9-1 红线），冷却渠道由 relay 层 failover 兜底（自检 F 段断言 SQL 未动）。自检 `coding-plan:test-cooldown-recovery` 31 断言 8 连跑幂等（A 解析 9 项/B 冷却写入/C pickAccount+recover/D 窗口恢复清冷却/E 调度过滤+到点切回/F static 零影响/G 零残留）；踩坑：断言精确比较 recordUsage 内部 time() 派生值遇跨秒间歇失败 → 时间容差 ±5s。顺手 pint 修两个历史遗留格式文件。回归：failover 24/24、cost-routing 24/24、time-discounts 28/28、currency 30/30、parser fixture、adapters-verify ALL PASS、pint 412 files PASS
- [x] P9-4 可观测与验收：管理端展示每模型各源「成本/状态/恢复倒计时」；手测断言 failover 与恢复回归路径（含 cost_first 下恢复后切回）✅ 2026-09-12（二十六）——管理端新页 `/admin/coding-plan`（侧边栏「Coding Plan 池」入口）：①账号池概览按 vendor 聚合（可用/冷却中/耗尽/停用）；②账号+渠道双级冷却倒计时（消费 P9-3 `cooldown_until`，秒级实时 JS）；③近 7 天跨源 failover 统计（消费 P9-2 `meta.route`：落选原因分布、承接 vendor 分布、最近 20 条明细含候选渠道/Vendor/`cost_per_1k` 成本）；failover 与恢复回归路径已由 test-cost-failover 24 断言与 test-cooldown-recovery 31 断言覆盖

### QA 全量功能自检（R13，2026-09-12 第十九轮新增）

> 用户要求：把**所有功能**自查一遍，所有错误/问题/业务逻辑缺陷记入本节问题表（QA-N 编号），逐条修复；每修复一批即 commit+push → 服务器 pull → 自动清理升级（migrate/缓存）→ **全部设置重新检测一遍**。会话中断后按本节继续。

**检查矩阵**（✅=通过 / ❌=有问题→见问题表 / ⏳=待检）：

| # | 类别 | 检查项 | 本地 | 服务器 |
|---|------|--------|------|--------|
| A1 | 工程 | `php -l` 全量（app/routes/config/database） | ✅ 0 错误 | ⏳ |
| A2 | 工程 | `vendor/bin/pint --test` 风格 | ✅ PASS | ⏳ |
| A3 | 工程 | `php artisan route:list` 可启动（549 条） | ✅ | ⏳ |
| A4 | 工程 | `migrate:status` 无 pending | ✅ 000016 Ran | ⏳ |
| B1 | 自检 | `coding-plan:test-time-discounts` 28 项 | ✅ | ⏳ |
| B2 | 自检 | `coding-plan:test-currency` 30 项 | ✅ | ⏳ |
| B3 | 自检 | `php tests/coding-plan-parser-fixture.php` 全过 | ✅ | ⏳ |
| C1 | 部署 | 服务器 git pull 至 origin/main | — | ✅ 已快进（QA-2） |
| C2 | 部署 | 服务器 migrate 无 pending | — | ✅ 000001→000016 全 Ran |
| C3 | 部署 | config/route/view 缓存重建 + storage 权限 | — | ✅ 当前未启用缓存（bootstrap/cache 仅 packages/services），storage 可写正常 |
| C4 | 运维 | schedule:run / queue:work 进程存在性 | — | ✅ 本轮落地（QA-1）：systemd 常驻 schedule:work + queue:work |
| C5 | 运维 | laravel.log 近期无新 ERROR/Exception | — | ⚠️ 发现 QA-11/QA-12 两处 500（已修复，待部署复检） |
| D1 | 网站 | 首页/介绍页 200 | — | ✅ https 200 + /api/status 正常 |
| D2 | 网站 | 登录 snails → /api/user/self | — | ✅ |
| D3 | 网站 | 用户侧接口抽测（令牌/额度/公告/订阅） | — | ✅ 令牌创建/self/额度读取通过（额度修改经 QA-11 修复后走 API） |
| D4 | 网站 | 公开 API：public_promotions / offers 数据 | — | ✅ 4 vendor（含 currency）+ 9 条活动返回 |
| D5 | 网站 | 管理端 API 抽测（dashboard/渠道/模型/设置/汇率/活动） | — | ⚠️ QA-11（PUT /api/user 500，已修）；渠道列表/选项/汇率读取 ✅ |
| D6 | relay | `/v1/models` 列表 | — | ✅ 返回模型列表（含 doubao-seed-evolving） |
| D7 | relay | `/v1/chat/completions` 真实调用（火山 ark，OpenAI 协议） | — | ✅ OpenAI↔Anthropic 协议转换真实调 ark，回复「正常」+ reasoning_content + usage 52/29 |
| D8 | relay | `/v1/messages` Anthropic 协议（ark /api/coding） | — | ✅ 非流式回复「正常」usage 52/29；流式 SSE 事件序列完整（message_start→content_block→thinking/text delta→message_delta→message_stop→[DONE]） |
| D9 | relay | 错误路径：无效 token 401 / 无效模型口径 | — | ✅ 无效 token 401 |
| D10 | relay | 用量落库与扣费（quota 扣减 + meta.fx） | — | ⚠️ 扣费链路四处对齐（logs/token/channel/user used=157；流式 +85）；发现 QA-13 流式客户端中断泄漏预扣额度（已修复并服务器验证 ✅，见 QA-13/QA-14）；logs.other 为空数组属预期（meta.fx 仅 CodingPlan 用量日志使用） |

**问题表**（修复完一条勾一条 `[x]`，状态：open / fixed / wontfix(注明) / 观察）：

| 编号 | 严重度 | 位置 | 现象与影响 | 修复 | 状态 |
|------|--------|------|-----------|------|------|
| QA-1 | 高(运维) | 服务器进程 | 初查（ps aux / root crontab / systemd timers / tmux / screen）**未发现** `schedule:run`、`queue:work` 任何进程——6h 官方源同步、每日 09:00 活动提醒、订单超时取消、任务轮询等调度链路疑似停摆 | systemd 双单元常驻落地（用户授权后本轮配置）：`pease-schedule.service`（`artisan schedule:work`，User=www，Restart=always——cron 每分钟 schedule:run 无法覆盖 task:poll 5s 级 sub-minute 任务，schedule:work 可覆盖）+ `pease-queue.service`（`artisan queue:work redis --sleep=3 --tries=3 --max-time=3600`）。验证：schedule:work 每 5s 触发 task:poll DONE；worker 启动即消化积压队列（印证堆积预判，2 封历史验证邮件入 failed_jobs 引出 QA-17）；调度器随即暴露 ability:fix 每小时任务崩溃（QA-18，已修复） | ✅ fixed |
| QA-2 | 中(部署) | 服务器 git | 服务器落后 origin/main 25 个提交（HEAD=3d1bb44 vs origin=80037b0） | 本轮部署 `git pull` 快进解决 | ✅ fixed |
| QA-3 | 高(迁移) | `CodingPlanCatalog::applyRatios()` | 全新库按序回放迁移时 000005 崩溃：ratio 写入未守卫 `time_discounts` 列缺失（该列由 000012 补） | applyRatios 对 time_discounts 增加 Schema::hasColumn 守卫；空库全量回放验证通过 | ✅ fixed |
| QA-4 | 低(工程) | tests/coding-plan-parser-fixture.php | 硬编码绝对路径 `/Users/snails/...`，其他环境/服务器无法运行 | 改为 `__DIR__.'/../vendor/autoload.php'` | ✅ fixed |
| QA-5 | 高(数据) | CodingPlanVendorSeeder | seeder 未写 currency 字段，按量价币种无法推导（USD/CNY 混排） | 补 anthropic/openai/google=USD、alibaba=CNY；服务器重跑后 vendor 4 行 currency 正确 | ✅ fixed |
| QA-6 | 中(设置) | Option UsdExchangeRate | 服务器 `UsdExchangeRate=1`（本地 7.3），USD 计价按量价折算 CNY 全错 | 用 snails 走管理端 `PUT /option/` 真实 API 改 7.3，GET 确认生效 | ✅ fixed |
| QA-7 | 低(自检) | TestCodingPlanCurrency | 断言失败时仍打印「全部通过」（29 通过 1 失败也报全绿），自检口径失真 | 对齐 time-discounts 的失败口径（非 0 退出 + 失败明细）；复测 30/30 | ✅ fixed |
| QA-8 | 中(安全) | 服务器 .env | 生产站点 `APP_ENV=local / APP_DEBUG=true / APP_URL 空`——异常会把堆栈/环境暴露给公网，且生成的 URL（支付回调/邮件链接）可能错 | 本轮配置（先备份 .env.bak-qa8-*）：APP_ENV=production / APP_DEBUG=false / APP_URL=https://www.peaseapi.com；config:clear 后 `artisan about` 确认 production/Debug OFF；首页与 relay 200 复测通过 | ✅ fixed |
| QA-9 | 中(运营) | 服务器 channels | 渠道表 0 行——relay 无任何上游，平台无法转发任何请求 | 属运营配置缺失非代码缺陷；本轮为 E2E 验证创建测试渠道 volc-ark-coding（volc ark coding，type=4） | 观察 |
| QA-10 | 中(功能) | Relay/Volcengine | ①`VolcengineAdapter` 是死代码（selectAdapter 把 38/56 归入 openAITypes→OpenAIAdapter），且其默认 base `/api/v3` 与 OpenAIAdapter 固定拼的 `/v1/chat/completions` 相加仍打不通 ark（会变成 /api/v3/v1/...）；②还会给模型名强加 `ark-` 前缀（doubao-seed-evolving 等会请求失败）；③OpenAI 协议上游 base_url 无法适配带版本路径网关（ark coding /api/coding/v3 等固定拼 /v1/...） | 根因确认为 **QA-15**（类型数组存枚举实例，与 int 比较恒 false——所谓"归入 openAITypes"从未生效，一切渠道都落 default OpenAIAdapter）：修复后按类型正确分发；ClaudeAdapter::formatRequest 同时去掉硬编码 api.anthropic.com 改走 getUpstreamUrl('/v1/messages')（尊重渠道 base_url/Coding Plan 覆盖，ark coding /api/coding/v1/messages 实测打通）。VolcengineAdapter 死代码本体留待重构（现 OpenAI 兼容网关经 OpenAIAdapter/ClaudeAdapter 均可用；③ 由 QA-23 版本段去重解决、本体已由 QA-19 删除） | ✅ fixed(根因) |
| QA-11 | 高(接口) | UserController::update | `PUT /api/user/` 500 ArgumentCountError：签名要求路由 {id}，但路由不提供（new-api 兼容约定 id 在 body）；laravel.log 已留痕 | update() 改 `int $id = 0` + body id 回退 + 校验对齐 Api\UserApiController（quota 替代无效的 balance 字段） | ✅ fixed |
| QA-12 | 高(接口) | ChannelService::syncAbilities | 创建渠道不带 priority 时 abilities 插入 null → NOT NULL 约束 500（SQLSTATE 23000），渠道根本建不出来 | `'priority' => $channel->priority ?? 0` | ✅ fixed |
| QA-13 | 高(计费) | RelayController::handleStream / 各流式适配器 streamHandler | 流式请求客户端提前断开（用户取消生成）时预扣 500 quota 泄漏。两层根因：①`ignore_user_abort=Off` 下 PHP 在输出检测到断连后直接终止脚本，catch/退款/计费全部跳过；②仅加 `ignore_user_abort(true)` 后脚本生命被绑定到上游生成长度——思考模型静默期无 chunk 可写，php-fpm 下 `connection_aborted()` 仅在写失败时置位（探针证实），请求存活到被 `request_terminate_timeout=100` SIGKILL（shutdown 函数不执行），结算依旧跳过 | 三层修复（50e5b3f / 5c87eb6 / 65a5f7b）：闭包入口 `ignore_user_abort(true)`；5 个流式适配器改 curl_multi 轮询循环（BaseAdapter::pollStreamTransfer）——WRITEFUNCTION 检测断连即 `return 0` 中止上游读取、每秒在 SSE 行边界写 `: keepalive` 注释行探活（SSE 规范：冒号开头的行客户端必须忽略）；RelayHandler::handleStream 对 clientAborted 立即 logConsume（按已解析 usage 结算并冲销预扣），RelayHandler::handle 非流式入口同样 ignore_user_abort(true)。实测：断连后秒级结算落库（仅计输入 58，预扣 500 全额退款；logs/request_count/used_quota 对账吻合），完整流式（887/837tok）与非流式（161）计费不受影响 | ✅ fixed |
| QA-14 | 高(运维) | php-fpm request_terminate_timeout=100 | 服务器 `/www/server/php/83/etc/php-fpm.conf` 硬性 SIGKILL 超过 100s 的请求（shutdown 函数不执行）——长思考模型完整流式（600 token 需数分钟）会在 100s 被杀：客户端看到连接重置、平台按已结算部分计费（若结算已跑完）或泄漏预扣（若未到结算点） | 本轮配置（先备份 php-fpm.conf.bak-qa14-*）：`request_terminate_timeout = 0`（选 0 而非 300：适配器 CURLOPT_TIMEOUT=300 封顶之下，300 硬限恰可能在最长流结算落库瞬间误杀——0 消除整类风险，请求生命周期由上游 curl 超时与断连检测自然收敛）；`php-fpm -tt` 解析确认 0s + USR2 平滑重载（master 重启 15 worker、站点/relay 200）。实证：max_tokens 8000 完整流 **213.9s**（>旧阈值 100s 两倍余）message_stop 干净收尾、usage 77+7378、logs id=23 记账 7455 分零泄漏 | ✅ fixed |
| QA-15 | 高(功能) | RelayHandler::selectAdapter | 类型数组（openAITypes/claudeTypes/geminiTypes/awsTypes/vertexTypes）存的是 **ChannelType 枚举实例**，与 int 的 `$channelType` 做严格 `in_array(..., true)` 恒 false——除协议匹配（Anthropic 原生入口）外，**所有渠道一律落入 default → OpenAIAdapter**：Claude/Gemini/AWS/Vertex 适配器不可达，type=4 渠道的 OpenAI 协议请求走原始透传而非协议转换（服务器反射实测 channelType=4 → OpenAIAdapter 证实） | 四处 in_array 前经 `enumValues()` 取 `->value` 再严格比较（642220d）；部署后反射复测 type=4 → ClaudeAdapter ✓ | ✅ fixed |
| QA-16 | 高(计费) | OpenAICompatibleTrait::streamHandler | OpenAI 兼容渠道（DeepSeek/Moonshot/ARK v1 等）流式为纯透传：`stream_options.include_usage` 已注入（supportStreamOptions 白名单）但 WRITEFUNCTION **从不解析 usage** → 流式日志 prompt/completion/quota 恒 0（服务器实测 OAI 完整流 0/0/0），免费送量 | WRITEFUNCTION 补 line-buffered SSE 解析：usage chunk → promptTokens/completionTokens/prompt_tokens_details.cached_tokens（对齐 OpenAIAdapter 已有实现，642220d）。修复后实测（type=4 经 ClaudeAdapter 转换路径）：OAI 完整流 50+1014tok=1064 分；OAI 中断仅计输入 55（completion=0）；四处对账 user=token=log_sum=4544 ✓；自检 30/30+28/28 ✓ | ✅ fixed |
| QA-17 | 高(邮件) | EmailService / 全体 Mailable | 管理端 SMTP options（SMTPServer/Port/Account/Token，库内已配 smtp.163.com）对**队列邮件从不生效**：入队时 web 进程 applySmtpConfig 后 `Mailer::queue` 把解析的 mailer 名 'smtp' 盖到 mailable 序列化（`$view->mailer($this->name)`），worker 按 'smtp' 名解析的是**自身 .env 静态配置**（遗留 MailHog 127.0.0.1:2525）→ 全部验证/重置邮件投递失败（failed_jobs 记录 8ms Connection refused）；且 MailManager 按名缓存 Mailer 实例，常驻 worker 后续热更 port=465 也不生效（重试仍打 587 被主机商封禁 20s 超时） | ①新增 `App\Mail\BaseMailable`（send 前在**发送进程**重放 options SMTP 配置）+ 3 个 Mailable 改继承（760d12d）；②applySmtpConfig 末尾 `MailManager::purge('smtp')` 强制按新 config 重解析（a486090）；③From 地址 filter_var 校验（SMTPFrom='PeaseAPI' 非法→回退 SMTPAccount，数据侧 SMTPFrom 修为 ruai@163.com）；④运营配置 SMTPPort 587→**465**（实测出站 587 被主机商封、465 可达，smtps 隐式 TLS 由 MailManager port==465 自动选择）。实测：验证邮件队列 **2 秒 DONE**（journal RUNNING→DONE），failed_jobs 恒 4 无新增 | ✅ fixed |
| QA-18 | 高(调度) | FixAbilities (ability:fix) | QA-1 落地调度器后立即暴露：`Channel.models` 为 array cast（JSON），`explode(',', $ch->models ?? '')` 直接 TypeError；且 `Ability::truncate()` 先于构建执行 → 崩溃时**能力表已被清空**（实测 0 行/4 活跃渠道，Distributor 按 model+group 查 abilities → 路由受损；该任务每小时执行且自部署以来从未成功，存量雷） | models 输入归一（array|逗号串双兼容）；行集先完整构建，再事务内 DELETE+重建（TRUNCATE 属 DDL 隐式提交不可回滚，DELETE 可回滚——中途异常不再清空旧表）（f4e2c21）。部署后手动执行 Created 4 abilities、abilities=4、relay 200 路由恢复 | ✅ fixed |
| QA-19 | 低(整洁) | VolcengineAdapter | 死代码本体（QA-10 遗留）：selectAdapter 从不分发（38/56 归 openAITypes→OpenAIAdapter，全代码库无实例化点），且 docblock 含杜撰模型名（SkyLint/CharacterGLM 等）与错误的 ark- 前缀强加逻辑，误导后续维护 | `git rm` 删除（type=38/56 渠道继续走 OpenAIAdapter，ark 网关 URL 由 QA-23 版本段去重打通）；自检脚本含「类不存在/文件已删」断言 | ✅ fixed |
| QA-20 | 高(功能) | GeminiAdapter (type=50/26/25) | 四连：①无 streamHandler（继承 BaseAdapter 空实现）→ 流式请求返回空流且计费恒 0；②`convertMessages` 把 system 消息映射为 model 角色（Gemini 无此角色）→ 上游必报错；③响应只取 `parts[0]` 单文本段、**完全丢弃 usageMetadata** → 非流式计费恒 0；④硬编码 generativelanguage.googleapis.com，渠道 base_url 网关无法使用 | 新增 `GeminiCompatibleTrait`（Gemini SSE→OpenAI chunk 逐块协议转换；usageMetadata 计费 prompt/candidates+thoughts/cachedContent；断连 WRITEFUNCTION return 0 + curl_multi 每秒探活，与 QA-13 修复同构）；system→systemInstruction 归位、assistant→model、多模态仅聚合 text 段、全 parts 聚合；formatRequest 走 `getUpstreamUrl('/v1beta/models/{model}:generateContent')`（尊重 base_url/模型映射）；errorHandler 转 OpenAI 错误格式。本地 mock SSE E2E 9 断言（URL/请求体/chunk/finish/usage 10-7-3/status 200）全过 | ✅ fixed |
| QA-21 | 高(功能) | VertexAdapter (type=21) | streamHandler 为阻塞 curl_exec 整段缓冲 SSE：从不调用 $callback（**客户端全程零输出**）、不解析 usageMetadata（流式计费恒 0）、connection_aborted 不感知（QA-13 同类缺陷）；非流式同样丢 usageMetadata | 接入 GeminiCompatibleTrait（与 QA-20 同一套转换/计费/断连感知）；`buildVertexUrl` 支持渠道 base_url 网关根覆盖（project/region 仍由 key 的 SA JSON 或 other 字段解析）；E2E 断言 URL(/v1/projects/.../:streamGenerateContent)/Bearer 认证/输出/计费全过 | ✅ fixed |
| QA-22 | 中(功能) | BaseAdapter::streamHandler 默认空实现 | 无流式能力的适配器（AWS/Task 等继承者）遇流式请求时**静默返回空流**：客户端收到空 200 + [DONE]，平台按 0 计费——流式黑洞伪装成成功，且无从告警 | 默认改 `throw RuntimeException('适配器[x] 不支持流式请求')` → RelayHandler::handleStream 捕获后 refundPreConsumed + 下发错误事件（诚实失败、零计费）；TaskAdapter 删除空覆盖继承之；自检含 BaseAdapter/TaskAdapter 双抛出断言 | ✅ fixed |
| QA-23 | 中(功能) | RelayInfo::getUpstreamUrl | base_url 带版本路径的网关拼双重版本 404（QA-10③ 遗留）：ark `/api/v3` + 固定 path `/v1/chat/completions` → `/api/v3/v1/...`；zhipu `/api/paas/v4` 同理；base 显式带 `/v1`（moonshot 式）则 `/v1/v1/...` | 服务器实证路由后实现**版本段去重**：base 末段为版本段且 path 以版本段开头时，同版本号去 base 版本段（moonshot `/v1`+`/v1/x`→`/v1/x`，实测 /chat/completions 404、/v1/... 401）、异版本号去 path 版本段（ark `/api/v3`+`/v1/x`→`/api/v3/x`，实测 401）；7 组拼接矩阵自检（ark/moonshot/ark-coding×2/zhipu/anthropic/dashscope 全过） | ✅ fixed |
| QA-24 | 高(计费) | AWSAdapter (type=22) | QA-15 修复后 type=22 才真正可达，暴露五连缺陷：①**从不设置 upstreamUrl**（curl_init('')→exec false→responseStatus=0→isError() false）→ 静默 200 空响应计 0 费；②formatResponse 丢 Bedrock Converse usage（inputTokens/outputTokens/cacheReadInputTokens）→ 非流式计费恒 0（QA-20 同类），且只取 content[0] 单段丢多段文本；③请求体误带 modelId 顶层字段且 messages 原样透传（Converse 要求 content:[{text}] 数组、system 需剥离）；④无 errorHandler（Bedrock 错误 JSON 原样透传，OpenAI 客户端无法解析）；⑤curl 失败时 exec=false 赋给 string 类型化属性抛 TypeError（extends Error），RelayHandler 仅 catch (Exception) → Error 冒泡跳过退款/计费 | 重写为 Bedrock Converse 协议：formatRequest 转 messages/system/inferenceConfig + getUpstreamUrl('/model/{modelId}/converse')（modelId 进 URL 路径，受 QA-23 去重约束）；doRequest 显式检查 exec false/status 0 抛 RuntimeException（杜绝 200 空黑洞）+ Timeout 300 + apiKey 优先 info；formatResponse 全段聚合 + usage 计费（cached=cacheRead 截断到 prompt）+ stopReason→finish_reason 映射；errorHandler 转 OpenAI 错误格式；流式仍不实现（converse-stream 为 AWS 二进制事件流非 SSE，QA-22 抛错兜底=诚实失败——遗留项「AWS 断连感知」就此关闭，未来实现必须复用 pollStreamTransfer）；连带 RelayHandler 两处 catch Exception→Throwable（TypeError 等也走退款+JSON 500）。自检 +19 断言 63 ✓ ALL PASS | ✅ fixed |

---



| 厂商 code | 产品 | 确定页面 | 格式 | 代理 | 状态 |
|---|---|---|---|---|---|
| deepseek | API 按量 | https://api-docs.deepseek.com/zh-cn/quick_start/pricing/ | HTML | 否 | ✅已接解析器（转置表） |
| openai | API 按量 | https://platform.openai.com/docs/pricing.md（.md 后缀=Markdown） | MD | **是** | ✅已抓取 |
| openai | 模型目录 | https://platform.openai.com/docs/models.md | MD | **是** | ✅已接（sync-official 目录 diff） |
| google | Gemini API | https://ai.google.dev/gemini-api/docs/pricing | HTML | **是** | ✅已抓取 |
| anthropic | Claude API | https://docs.anthropic.com/en/docs/about-claude/pricing | HTML | **是** | ⚠️区域封锁，备选 .md |
| xai | Grok API | https://docs.x.ai/developers/models.md | Markdown | **是** | ✅已解析 |
| zhipu | GLM Coding Plan | https://docs.bigmodel.cn/cn/coding-plan/overview（llms.txt 索引） | Mintlify | 否 | ✅已抓取 |
| aliyun | Token Plan | https://docs.bailian.console.aliyun.com/llms-full.txt（源重定位；单页 .md 被 WAF 拦） | Markdown 拼接 | 否 | ✅已抓取（内容=套餐档位，归 P2-1/P7） |
| tencent | TokenHub Token Plan | https://cloud.tencent.com/document/product/1823/130060 | HTML | 否 | ✅已抓取 |
| siliconflow | 模型价格中心 | https://siliconflow.cn/pricing | HTML | 否 | ✅已抓取 |
| moonshot | Kimi API | https://platform.kimi.ai/docs/pricing/chat.md（moonshot.cn 301 → kimi.com；国际站 $） | Mintlify .md | **是** | ✅已解析（4 模型，与预置零 diff） |
| volcengine | 方舟模型服务/Agent Plan | https://docs.volcengine.com/api/doc/getDocDetail?DocumentID=1544106&lang=zh（旧 1099320 已 301 → 1544106） | JSON（Quill delta） | 否 | ✅已解析（38 模型目录「新增 38」；按量价 CNY 归 P7-1） |
| unicom | Coding/Token Plan | https://support.cucloud.cn/document/127/591/2357.html?id=2357&arcid=7015 / 7080 | 页面内嵌 totalList 全站文档树 | 否 | ✅目录 11（新增 10，2026-09-12）；档位 CNY→P2-1/P7 |
| scnet | Token Plan | https://www.scnet.cn/ac/openapi/doc/2.0/moduleapi/plans/token-plan.html | VitePress SSR HTML | 否 | ✅目录 19（新增 19，2026-09-12）；Credits 档位 CNY→P2-1/P7 |
| siliconflow | 按量价目 | https://siliconflow.cn/pricing | SSR HTML（Tailwind 静态） | 否 | ✅目录 48（新增 48，2026-09-12）；¥/M tokens CNY 双时段价→P7-2/3 |
| cmcc | Coding/Token Plan | https://ecloud.10086.cn/op-help-center/doc/article/98322 、/doc/outline/108724 | React SPA（cloud-cms-service-web） | 否 | ⏳文档 API 需 OIDC 登录态（匿名 302→iam/oidc/authorize），预置数据覆盖 |
| minimax | API 按量 | https://platform.minimax.io/docs/guides/pricing-paygo.md（国际站 $；国内站 ¥） | Mintlify .md | **是** | ✅已解析（3 模型「新增 3」） |
| baidu | 千帆 | https://cloud.baidu.com/doc/qianfan/s/wmh4sv6ya（旧 hlpl7xe2f 已 302） | HTML（SSR） | 否 | ✅已解析（31 模型目录「新增 30」；按量价元/千 tokens CNY 归 P7-1） |

---

## 6. 变更记录

- 2026-09-12（二十五）：**P9-3 恢复回归（渠道级 cooldown_until）落地**——①迁移 000017：`coding_plan_accounts.cooldown_until` + `channels.cooldown_until`（unsigned int 默认 0）。②`RelayInfo::resolveQuotaCooldown()` 四级解析（Retry-After 头秒数/HTTP 日期 → JSON reset_* 秒/毫秒/日期 → 中英文案 → null）；handle 失败路径 quota_exceeded 传入 recordCodingPlanUsage 第三参 → recordUsage 写账号冷却，派生序=上游解析值>窗口恢复点>保守默认 now+5h，修复 quota_*=0 账号被 429 打标后无恢复点「永耗尽」缺口（resetExpiredWindows 永不处理）。③PoolService 新增 recoverExpiredCooldowns（pickAccount 前置恢复）+ refreshChannelCooldown（池可用清渠道冷却/全不可用=最早恢复点，正常耗尽、quota_exceeded、窗口恢复后均同步）。④candidateChannels 过滤冷却渠道：cost_first 恢复到点自动切回低成本源 + failover 候选天然跳过；static pickChannel SQL 零改动（P9-1 红线），由 relay 层 failover 兜底。⑤自检 `coding-plan:test-cooldown-recovery` 31 断言 8 连跑幂等。踩坑：自检断言精确比较 recordUsage 内部 time() 派生值遇跨秒间歇失败 → 时间容差 ±5s。顺手 pint 修 TestCodingPlanTimeDiscounts/CodingPlanModelRatio 历史格式问题（全仓库 412 files PASS）。回归：failover 24/24、cost-routing 24/24、time-discounts 28/28、currency 30/30、parser fixture、adapters-verify ALL PASS。
- 2026-09-12（二十七）：**P1-9 定价源健康告警落地**——①升级标记：`CodingPlanRatioCheck::SOURCE_FAILED_ALERT=3`（零迁移常量）；`resolveSourceStatus(vendor, status)`：传 SOURCE_FAILED 时查同 vendor 最近一条流水（checked_at+id 双倒序），前条为 FAILED/ALERT 即升级 ALERT，成功一次自然复位，sync-official 与 verify-ratios 两条写入路径统一接入；命令行输出同步区分两态。②数据面：`sourceHealth()` 取流水最近 500 条按 vendor 分组推导 last_status/连续失败数（自最新回数失败态，首个 OK/NONE 截止）/last_success_at/last_checked_at/alerting。③管理端：`/admin/coding-plan` 页首「官方源同步健康」卡片（告警态 border-danger+「● N 个源连续失败告警」红 badge，行级 table-danger、连续失败次数红色加粗、最近成功/最后检查时间）+ `layouts/dashboard.blade.php` 侧边栏「Coding Plan 池」入口红点 badge（`Cache::remember('admin:cp_source_alert_badge', 300)`，try/catch 兜底 0 不阻塞渲染）。④自检 `tests/source-health-verify.php` 14 断言（升级序列 F→A→A/复位/单次失败不升级/OK 原样/sourceHealth 汇总/完整 render 两态断言（告警红 badge+table-danger+侧边栏 bg-red-500；复位后「全部正常」+红点消失）/回滚零残留；注意自检会写 badge 缓存，finally 必 forget）。回归：pint 6 文件 PASS、verify-ratios 5 vendors 0 failure、P9-4 自检、adapters ALL PASS、parser fixture、cost-routing 24/24、cooldown 31/31 全绿。
- 2026-09-12（二十六）：**P9-4 管理端可观测落地**——①新页 `/admin/coding-plan`（`AdminController::codingPlan` + `admin/coding-plan.blade.php` + 侧边栏「Coding Plan 池」入口）：账号池概览（vendor 聚合 available/cooling/exhausted/disabled，SQL case-when 一次聚合）；账号+渠道双级冷却倒计时（`data-deadline` + 原生 JS 秒级 tick，到点显示「已恢复」）；近 7 天跨源 failover 统计（`coding_plan_usage_logs.meta` cast array 取 `route`：failover 计数、by_reason（pool_exhausted/apply_failed）、by_final_vendor（流水 vendor=最终承接）、recent 20 条明细逐候选展示 channel_id/vendor/`cost_per_1k`（static 无成本显示占位）/reason）。②自检 `tests/admin-observability-verify.php` 12 断言（事务内造最小管理员+3 状态账号+cost_first/static 双策略 failover 流水+渠道冷却打标 → 控制器取数 → **完整 render() 验证 blade 编译与 HTML 内容** → 回滚零残留）。踩坑：①`layouts/dashboard.blade.php` 无条件读 `auth()->user()`，CLI 自检须事务内造管理员并 `Auth::login` 后再渲染；②users 表 `aff_code` NOT NULL 无默认（32 长度 unique）；③本地 channels/users 表为空——渠道冷却断言设计为「存在渠道才断言」，空库跳过该分支（生产有数据时生效）。回归：P9-4 自检 12/12、failover 24/24、cost-routing 24/24、time-discounts 28/28、currency 30/30、cooldown 31/31、parser fixture、adapters-verify ALL PASS、pint PASS。README 同轮全面改版（双池/Cost 路由自愈/新闻搜索聚合/自检清单/Gitee 镜像徽章）并双推 GitHub+Gitee。**生产部署 3d1c7c8**（含 ClaudeAdapter model 属性修复，无迁移）：git pull → view/route/config clear → fpm master USR2 平滑重载；在线自检全绿——admin-observability 14/14（服务器有真实渠道数据，渠道冷却双断言在本地空库跳过、生产生效）、adapters ALL PASS、parser fixture、cooldown 31、failover 24、cost-routing 24、time-discounts 28、currency 30、verify-ratios 4 vendors 0 stale/0 源失败；探活 https://www.peaseapi.com/ 200、/admin/coding-plan 未登录 302（路由注册健康）。

- 2026-09-12（二十四）：**P9-2 跨源 failover 落地**——①`RelayHandler::establishUpstreamChannel()`（handle/handleStream 接线）：正常路径与 `applyCodingPlanAccount` 零差异，仅「account pool exhausted」RuntimeException 触发 failover（订阅校验失败等原样上抛）；`failoverChannel()`：候选=`candidateChannels`（P9-1 预留）排除已败渠道、每候选 setChannel→setModel 重映射（model_mapping 逐渠道不同）→selectAdapter→applyCodingPlanAccount，RuntimeException 落选记 attempted 继续，序=cost_first 成本升序（sortChannelsByCost）/static priority desc+id asc，上限 5 个；普通 API 渠道天然通过=兜底归宿；全败 throw 原始异常走既有退款路径（catch Throwable 不变）。②`RelayInfo::routeDecision`（reason=cost_failover/strategy/failed/final/attempted[]含 cost_per_1k）经 recordCodingPlanUsage 并入 `coding_plan_usage_logs.meta.route`（仅 failover 时非空）。③修复：setModel 重映射未命中重置 isModelMapped（换渠道映射状态残留）；applyCodingPlanAccount 非池分支清残留 codingVendor/codingPlanAccount（池耗尽抛异常前已设 vendor）。**踩坑**：RelayHandler 原 use 无 RuntimeException→catch(RuntimeException) 解析为不存在类永不命中（php -l 不报错，自检抓出）。④自检 `coding-plan:test-cost-failover` 24 断言三轮幂等。回归：cost-routing 24/24、time-discounts 28/28、currency 30/30、parser fixture 全过、adapters-verify ALL PASS、pint PASS。现网安全：ModelRouteStrategy 保持 static，failover 候选=priority 序、正常路径零差异。

- 2026-09-12（二十三）：**P9-1 成本感知路由排序器落地**——①新增 `app/Services/CostRouteService.php`：同模型多渠道候选按「每 1k tokens 平台成本」升序，口径与计费链严格同源（CodingPlanRatioService::calcUsage 同语义：resolve exact>prefix>全局默认、Asia/Shanghai 时段折扣、effectiveExchangeRate 账号级>vendor>1 折算积分）；探针=每 1k tokens（750 输入/250 输出、缓存命中不假设），per_token_parts 三段全 0 回退按次防资损；渠道→厂商经 coding_plan_accounts.channel_id（与 applyCodingPlanAccount 同口径），无账号关联渠道成本不可算垫底；币种因子已隐含于 unit_exchange_rate（防双重折算），cny_reference（官方币种市场汇率）仅观测输出供 P9-4。②`ModelRouteSetting` + Option 键 `ModelRouteStrategy`（static 默认/cost_first，非法回退）。③`ChannelSelectService::pickChannel` cost_first 分支接线（static SQL 原样零改动）+ `candidateChannels()` 供 P9-2 复用。④自检 `coding-plan:test-cost-routing` 24 断言事务内零残留。**踩坑**：flushCache 只清目标实例 memo（测试需重建实例）；flushCache 全量枚举自 vendors∪ratios 表，回滚后自检 vendor 缓存键漏清跨轮污染（显式 forget+断言防回归）。回归：cost-routing 24/24（三轮幂等）、time-discounts 28/28、currency 30/30、parser fixture 全过、pint PASS。
- 2026-09-12（二十二）：**QA-24 AWSAdapter 收口 + 遗留三项处置**——①重写 AWSAdapter 为 Bedrock Converse 协议（QA-15 修复后 type=22 才真正可达，暴露五连缺陷：upstreamUrl 恒空→200 空响应计 0 费、usage 全丢计费恒 0、请求体不符 Converse 结构、无 errorHandler、curl false→TypeError 冒泡跳过退款，详见问题表 QA-24）；②遗留「AWS 适配器断连感知」关闭：适配器无流式实现（converse-stream 为 AWS 二进制事件流非 SSE），流式请求经 QA-22 默认抛错兜底=诚实失败无断连问题，未来实现时必须复用 BaseAdapter::pollStreamTransfer（QA-13 同构）；③git identity 配置 repo 级 `Daydream <snails@192.168.1.76>`（与历史 8/8 提交一致），commit 不再提示；④遗留「Gemini/AWS/Vertex 真实 key 可达验证」保持开放（生产无此类渠道，需先建测试渠道+真实 key 方可实证）。验证：relay-adapters-verify **63 ✓ ALL PASS**（QA-24 组 19 断言：协议转换/usage 计费/cached 截断/stopReason 映射/errorHandler/URL 构建/流式与空 URL 显式抛错）、time-discounts 28/28、currency 30/30、pint PASS；服务器同步后 verify 复测 ALL PASS + fpm USR2 平滑重载。

- 2026-09-12（二十一）：**适配器层收尾（QA-19~23）：删 VolcengineAdapter 死代码、Gemini/Vertex 流式黑洞修复、不支持流式显式报错、URL 版本段去重**——①QA-20/21 探查发现远比预期严重：GeminiAdapter 无 streamHandler（空流+计费 0）、system 消息错映射 model 角色、丢 usageMetadata、硬编码域名；VertexAdapter 流式整段缓冲且从不调用 callback（客户端零输出）——两者共用新增 `GeminiCompatibleTrait` 修复（SSE→OpenAI chunk 转换、usageMetadata 计费 prompt/candidates+thoughts/cachedContent、断连感知+curl_multi 每秒探活与 QA-13 同构、base_url 网关支持）。②QA-22 BaseAdapter::streamHandler 默认空实现改 throw（不支持流式的适配器不再静默空流，handleStream 捕获后退款+错误事件）。③QA-23 服务器实证路由（ark /api/v3/chat/completions 401 存在、moonshot /chat/completions 404 必须 /v1）后实现 getUpstreamUrl 版本段去重（同版本去 base 段/异版本去 path 段），QA-10③ 收口。④QA-19 git rm VolcengineAdapter（全库无实例化点，docblock 含杜撰模型）。验证：新增 tests/relay-adapters-verify.php **48/48 ALL PASS**（本地 mock SSE 子进程 E2E：Gemini/Vertex 双流式 URL/Bearer/请求体/chunk/usage 10-7-3/[DONE]/status 200 + selectAdapter 6 分发 + URL 矩阵 7 + 抛出 2）；parser fixture 全过 + test-currency 30/30 + time-discounts 28/28 + pint PASS（8 files）；qa-19 前置：php -l 8 文件 0 错误。部署后服务器复测（HEAD=05ac257）：relay-adapters-verify 48/48 ALL PASS（期间发现服务器 CLI ini 的 mbstring 重复加载警告会污染 mock 子进程 stderr 握手——已改为循环跳过噪音行）；fpm USR2 平滑重载 + queue/schedule active；真实 relay 双探针：非流式 200「收到」/usage 52+23/记账 75 分（log#24）、流式 247 SSE 行首 delta 末 [DONE]/usage 49+400/记账 449 分（log#25，is_stream=1）——relay 端到端与计费在修复后正常。

- 2026-09-12（二十）：**QA-13/15/16 收官复检 + 三项服务器配置落地 + 邮件/调度两雷修复（QA-17/18）**——①三项配置（用户授权后执行，全部先备份）：QA-14 php-fpm request_terminate_timeout 100→0（`php-fpm -tt` 确认 + USR2 平滑重载；**213.9s 长流实证** message_stop 干净收尾 + 7455 分全额记账零泄漏）、QA-8 .env production/false/https URL（`artisan about` 复核 + 站点/relay 200）、QA-1 systemd 双单元 pease-schedule（schedule:work，覆盖 task:poll 5s sub-minute 级）+ pease-queue（queue:work redis --tries=3 --max-time=3600）常驻，worker 启动即消化积压队列（印证「任务堆积」预判）。②**QA-17 邮件链路三连根因**：worker 按入队时盖章的 'smtp' 名解析自身 .env 静态配置（MailHog 127.0.0.1:2525 遗留）→ BaseMailable 在发送进程重放 options SMTP（760d12d）；MailManager 按名缓存 Mailer 实例 → options 热更永不生效 → purge('smtp')（a486090）；出站 587 被主机商封禁（163/qq 双探针 20s 超时、465 可达）→ options SMTPPort 改 465 + SMTPFrom 数据修正（'PeaseAPI'→ruai@163.com，代码侧补 filter_var 校验 + SMTPAccount 回退）。终态：验证邮件队列 2 秒 DONE、failed_jobs 恒 4 无新增。③**QA-18**：调度器上线后 ability:fix 首跑即崩（Channel.models array cast vs explode + truncate 先于构建 → abilities 表曾被清空、Distributor 路由受损）→ 输入归一 + 事务内 DELETE 重建（f4e2c21），手动执行恢复 4 行、relay 200。④收官验证：对账 Δuser_quota=Δlog_sum=15760 分（4999000→4983240）、request_count 23（5 新请求 id19-23 全额记账 11216 分：282+278+122+3079+7455）；自检 test-currency 30/30 + **coding-plan:test-time-discounts** 28/28（注意命令已改名，旧名 test-time-discounts 触发 artisan 交互确认会挂起脚本）；pint PASS；/tmp 探针零残留；服务器 HEAD=a486090 与本地一致。**遗留交用户**：主机商封禁出站 587（SMTP 必须走 465/smtps 或改 API 投递商）；failed_jobs 保留 4 条历史失败（旧验证码已过期，故意不 retry）；本地 git committer identity 未配置（每次 commit 有提示）。
- 2026-09-12（十九）：**QA 全量功能自检启动（R13）**——按用户要求全功能自查+逐条修复+每轮 commit→push→服务器 pull→清理升级→复检；本地基线先验证：php -l 全量 0 错误、pint PASS、route:list 549 条、migrate 000016 齐、time-discounts 28/28、test-currency 30/30、parser fixture 全过 ✅。任务账本新增「QA 全量功能自检」节（检查矩阵 A/B/C/D 四类 20+ 项 + 问题表）；初查疑点：服务器无 schedule/queue 进程（QA-1）、服务器落后 25 提交（QA-2）。火山 ark 真实调用 key 已由用户提供（doubao-seed-evolving 等 10 模型，OpenAI 兼容 /api/coding/v3 + Anthropic 兼容 /api/coding）。
- 2026-09-12（十八）：**P8 上架流全量（P8-1/2/3）**——①后端 P8-1：`GET /coding_plan/vendors/{code}/models` 聚合清单（库内比率行全量 × 最新官方目录快照 × 最近校对流水的 model_catalog 条目）；每行 `official` 四态实时判定（in_catalog/missing/new/unknown，diffCatalogModels 同口径：仅 exact 行参与存在性比对、小写归一、prefix 不做前缀推断——prefix 行覆盖的官方模型仍报 new 为既有口径）+ new 虚拟行（id=null、model 取快照原样大小写）；summary 计数 + 快照 fetched_at。`CodingPlanOfficialSourceService::latestCatalog()` 抽取目录读取。②后端 P8-2：`POST /coding_plan/vendors/{code}/models/batch_status`（ids+status 批量启停，vendor 隔离）→ flushCache + `Cache::forget('channel_cache')`（与 SyncChannelCache 同口径，渠道可见性即时生效）。③后端 P8-3：`POST /coding_plan/catalog_changes/apply`（action=new 落地停用态 exact 行 per_request unit_cost=1 remark「待配置定价」，status=0 不自动计费；action=missing 停用对应行 + 写 `model_catalog|model|` 忽略键防重复提醒——停用行仍在后续 diff 中）；复用 ignoreCheckChange 的 IGNORE_KEYS_OPTION 双态兼容。④前端：新「模型上架」tab（models-tab.tsx）：厂商 Select → summary 徽标行（启用/停用/官方新增/官方未列/目录数+快照时间）→ 全选/勾选批量启停条 → 清单表（official 徽标 + new/missing 未忽略行 amber 高亮 + 未落地「落地（停用态）/忽略」、官方未列「下架（停用）/忽略」按钮，复用 ignoreCheckChange）。types.ts 增 OfficialModelState/CodingPlanVendorModelRow/CodingPlanVendorModels、api.ts 增三函数、index.tsx 挂 tab。**环境备忘**：本机无全局 node/Docker daemon 未随系统启动——typecheck 用 `node_modules/@typescript/native-preview-darwin-arm64/lib/tsgo -b`（原生二进制免 node）；oxlint/build 用 Docker（daemon `open -a Docker` 手动拉起，镜像走 `docker.m.daocloud.io` 加速，容器内 `npm i --no-save @oxlint/binding-linux-arm64-musl` 补平台 binding 后跑 lint/build；宿主 node_modules 是 darwin binding 不能直接在容器复用）。自检 verify_p8 32 项全过（openai 真实基线 + 事务内四态判定 + 批量启停/channel_cache 失效 + new 落地原样名/幂等 skipped + missing 停用+忽略键落库 + 401 隔离 + 回滚与快照/忽略清单清理）；回归 fixture ✓、time-discounts 28/28 ✓、currency 30/30 ✓、pint ✓、typecheck ✓、oxlint coding-plan 0 warning 0 error、build ✓。openai 现库 official 分布 {unknown:2,new:1}（最新快照 catalog 仅 1 条，历史同步状态非代码问题）。**P8 全部完成**

- 2026-09-12（十七）：**P7-5 收尾 + P2-4 + P4-1（介绍页）**——①后端 P2-4：公开端点 `GET /api/coding_plan/public_promotions`（enabled 且未过期、字段白名单、scheduled/ongoing 展示）；与同 URI 的管理端 promotions 路由会互相覆盖（Laravel 同 method+URI 仅存一条），故独立命名，自检确认管理端 401/公开 200 双轨。②后端 P7-5：`publicOffers()` 卡片增 `currency`（vendorOfficialCurrency 统一口径）、tiers 增 `currency`/`price_cny`（CurrencyExchangeService 折算 CNY，不可折算→null）；storeRate/destroyRate 补 `Cache::forget('coding_plan_offers')`（vendor 写入已走 flushCache 覆盖）。③前端 P4-1：公开介绍页 `/coding-plan`（routes/coding-plan/index.tsx + features/coding-plan-introduce/index.tsx）：官方活动区（kind 徽标 + 倒计时徽标 ≤72h 红/≤7 天黄/其余灰 + 截止时间 + 官方公告外链，即上下架公告位）、厂商卡片（plan_kind 分组、billing/currency 徽标、最近核对 + source_status + stale/change 计数、文档外链）、档位表（官方原币价/折算 CNY 双列）、模型抵扣率表。**路由冲突排障**：`_authenticated/coding-plan`（pathless layout alias）与新公开页同 URL → generator 报 Conflicting configuration paths 且 build 静默跳过 routeTree 重生成；管理端挪至 `_authenticated/coding-plan-manage`（use-sidebar-data 同步），routeTree 经一次性 Generator 脚本重生成。④自检 verify_p24 22 项全过（USD 100→730 折算正确、事务回滚零残留）。typecheck ✓ oxlint 0 error build ✓ pint ✓。**遗留**：coding-plan 页（管理端+介绍页）i18n → P4-3 统一改造；现库启用 vendor 均未录 tiers，双列价待运营补数。
- 2026-09-12（十六）：**P7-5 管理端前端（第一部分）**——①vendors-tab：VendorForm/payload/表格加 `currency`（CNY/USD/HKD/EUR/JPY/GBP/SGD select + 币种列，编辑回填 v.currency）。②新增「汇率」tab（rates-tab.tsx）：rates CRUD 表格（币种/汇率/生效值/来源/更新时间/备注）+ hints 提示区（CNY 基准恒 1、USD 无表值走 Option UsdExchangeRate 兜底，直接可见生效值）；upsert 语义保存（code 为主键）。③新增「厂商活动」tab（promotions-tab.tsx）：全字段 CRUD（vendor datalist 复用 vendors、kind/discount/datetime-local 起止（留空=未公布）/source_url/status/remind_days/sort/remark）+ state 徽标（scheduled/ongoing/expired）+ 倒计时列（remaining_seconds→「剩 X 天 Y 小时」，null=长期/未公布）+ 「含停用/过期」Switch（all=1）。④index.tsx 挂两 tab、types.ts 增 CurrencyRate/CodingPlanPromotion/PROMOTION_KINDS/PROMOTION_STATES、api.ts 增 rates/promotions 六函数。typecheck ✓、oxlint 0 warning、build ✓。**P7-5 剩余**：档位/介绍页原币价+折算价双列、介绍页倒计时徽标（需后端公开只读端点）、coding-plan 页 i18n 统一改造。

- 2026-09-12（十五）：**P2-3 活动提醒链路**——①迁移 000016 流水表 `coding_plan_promotion_reminders`（unique(promotion_id,kind,user_id) 防重发，user_id=0=站内公告全局）。②`CodingPlanPromotionReminder` 模型 + `STATUS_EXPIRED=2`（到期自动置过期，前台 enabled 口径自然退出）。③`PromotionReminderMail`（ShouldQueue）+ blade。④命令 `coding-plan:remind-promotions --dry-run`：临期 → Option「Notice」追加段落（用户端已有 GET Notice 消费）+ 订阅对应厂商套餐的有效用户（subscriptions⋈plans.coding_vendor，status=1 且 period_end≥now）队列邮件；过期 → 置 2 + expired 流水。⑤调度 dailyAt('09:00') 守卫注册。**踩坑复盘**：a) 验证脚本最初用外层事务回滚保护 Notice 写入——`Option::get` 走 `Cache::remember(60s)`，事务内读值回滚后残留缓存 60s 导致「行未回滚」假象；**断言一律 DB 直查 + 脚本首尾 Cache::forget('option:Notice')**。b) 自检脚本漏 `use App\Mail\PromotionReminderMail`，`::class` 解析为不存在的全局类名 → `Mail::queued()` 恒 0，排查走了大弯（minimal 复现+命令 stats 输出才定位）。c) dry-run 曾误改 status（过期分支 update 未守卫 dryRun）——**--dry-run 必须 100% 只读**。自检 14 项全过；回归 time-discounts 28/28、currency 30/30、fixture ✓；pint 全量跑捎带 3 个历史文件 style 改动 → `git checkout --` 还原保持提交纯净。**P2 全部完成（P2-1/2/3 ✓）→ 剩 P7-5 前端（vendors-tab 币种选择 + 汇率/活动维护页 + 介绍页倒计时徽标 i18n）**。

- 2026-09-12（十四）：**P2-2 已知活动预置**——迁移 000015 预置 9 条活动（TASKS 8 条 + cmcc 首订特价 7.9/39.9 元至 2026-12-31，Catalog cmcc() 注释背书）：kind 分布 discount×6 / price_change×1（阿里云个人版限时价 39/139/499 原价 60/180/600）/ model_retirement×2（腾讯 GLM-5/5.1 2026-10-09、xAI Imagine 2026-11-02）；每日循环型（智谱夜间畅用/阿里云夜间五折/限时价未公布截止）ends_at=null，有截止按北京时间 23:59:59 落 Unix 秒（\Carbon\Carbon::parse(..., 'Asia/Shanghai')）；discount=0.5 仅阿里云夜间五折与火山 Auto 系数两条（zhipu/google/cmcc 折扣非乘数口径留 null，说明写 description）；全部注 source_url。幂等：vendor+title firstOrCreate 不覆盖管理员行，remark「官方活动（预置 2026-09-12）（P2-2）」标记 down 可清。自检 9 项（9 条/kind 分布/ends_at 时区/全 ongoing/API 9 条+倒计时最紧=腾讯 GLM 下线）。**P2 进度：P2-1 ✓ → P2-2 ✓ → P2-3（提醒链路：每日 09:00 扫描 remind_days 内到期 → 站内公告+邮件+介绍页徽标）待做**。

- 2026-09-12（十三）：**P2-1 promotions/套餐档位落地**——①迁移 000013 新表 `coding_plan_promotions`（vendor/kind=discount,free,price_change,model_retirement/title/description/discount(0,1)/starts_at/ends_at(NULL=官方未公布)/source_url/status/remind_days(默认7)/sort/remark）+ 模型 `CodingPlanPromotion`（KIND_/STATUS_ 常量、displayState()→scheduled/ongoing/expired、remainingSeconds() 倒计时 null=无截止）。②管理 API 三路由对齐 rates：GET `/coding_plan/promotions`（?vendor=&all=1，行附 state/remaining_seconds）、POST（upsert 传 id 即更新；ends_at 0/null→NULL；ends_at<starts_at 422）、DELETE `/{id}`。③SCNet 套餐档位落地：`CodingPlanCatalog::templates()` 新增 `scnet` 模板（厂商行 status=0 默认停用；四档 30/110/265/764 元 CNY 活动价+原价 50/185/440/1274 price_note；月度 Credits 6 万~180 万两端已录、中间档快照未取到留空待核对；倍率示例 glm-5.3=2.29/kimi-k3=4.12 status=0）+ 迁移 000014 `CodingPlanCatalog::apply('scnet')`（与 000006 同构、apply 幂等、vendor/tier 币种补 CNY）。**踩坑复盘**：首版 000014 直插 vendor_tiers 与 000006/000007 既有 unicom/aliyun 精确预置（含额度 600/1200/1800 万 tokens、25000/100000/250000 credits）重复——模板通道是唯一权威数据源，档位预置一律走 templates()+apply()。P2-2（已知活动预置）、P2-3（提醒链路）待做。自检 17 项（scnet 档位/幂等重放/promotion 状态推导/API 端到端/零残留）。回归：fixture 全过 ✓、time-discounts 28/28 ✓、test-currency 30/30 ✓、routes 34 条 ✓、pint PASS。

- 2026-09-12：建立账本；完成 P0（首轮 9+ 家官方源抓取、快照预存 `docs/upstream-snapshots/2026-09-12/`）；确认 OpenAI/Google/xAI/Anthropic 需代理（本机 `http://127.0.0.1:7890` 已验证可用）；发现 OpenAI 文档页支持 `.md` 后缀直取 Markdown（P1-2 关键技巧）。
- 2026-09-12（二）：新增需求 R9-R12 → P7 多货币与结算体系 / P8 厂商模型上架流 / P9 成本感知路由与配额恢复感知（复用点已探明：`CodingPlanAccount` 5h/周/月窗口 + STATUS_EXHAUSTED 自动恢复、`ChannelSelectService` 静态调度、`UsdExchangeRate`、前端 `currency.ts`）。P1 核心工程落地：`CodingPlanOfficialSourceService`（15 家源注册表 + 代理抓取 `PEASE_API_HTTP_PROXY` + 快照保留 10 份 + 复用 diff/忽略/流水）+ `coding-plan:sync-official` 命令（每 6h 先于 verify）+ deepseek/openai 解析器（deepseek 真实结构=转置表；openai 代理链路解析 67 模型 → 「新增 67」待人工确认）+ fixture 测试 21 项断言（`tests/coding-plan-parser-fixture.php`）。发现并修正：快照实际落 `storage/app/private/`（Laravel 11 local disk root）；diffEntries 需兼容 list 输入（数字索引 key 误配）；停用行（status=0）不应报「新增」。回归：test-time-discounts 28/28、verify-ratios 无回归。
- 2026-09-12（三）：P1-2 再落三家解析器并全链路真抓验证（5/5 厂商 0 失败，共 116 处待确认变更）：google（SSR 中文机翻；锚点 id 模型名 + standard 层表；双价「起为」取恢复价防 2027-01-01 刷屏；分档长上下文首档；存储价「/小时」「每张图片」换算价与模态列表「/ 图片」噪声精确区分 → 28 模型）、anthropic（Next.js SSR div 表无 `<table>`，`<tr>` 平铺扫描 + 「Model」表头行重置列定位；显示名转 id；Batch/1M/CCU 表跳过同模型首条为准 → 17 模型）、zhipu（Mintlify `.md` 直取；页面=套餐积分配额无按量价 → `catalog_from_pricing` 注册表开关复用响应体产 model_catalog，U+2011 归一化 → 目录新增 4）。基建：`htmlTables()` 基类助手；sync 命令目录解析提前到条目判空前（entries 空 + catalog 空才算 SOURCE_FAILED）；fixture 扩至 42 项断言。回归：test-time-discounts 28/28、verify-ratios 无回归、pint PASS。
- 2026-09-12（四）：P1-2 再落 xai/tencent 两家解析器并全链路真抓验证（7 厂商 0 失败）：xai（**发现 `.md` 直取技巧同样适用**——原 /docs/models 已 308 → /developers/models.md；Text API Pricing 表长上下文分档取 < 200k 首档、≥200k 条件价跳过；Imagine/Voice 按次计价表跳过 → 7 模型「新增 7」）、tencent（Slate SSR；套餐积分配额页无按量价 → `catalog_from_pricing`；Model ID 表逐变体拆分 + 「整格全 ID」规则滤概览/工具格；GLM-5/5.1 下线经 catalog missing 呈现 → 目录新增 16）。**配置修复**：`.env` 补 `PEASE_API_HTTP_PROXY`（上轮代理仅临时环境变量，导致本轮境外源全超时误报抓取失败）。**工程决策**：siliconflow 结构摸清（pricing-row-{text|image|audio|video} 行 + `title="vendor/model"` + 「费用发生时段」双时段价组）但 ¥/M tokens 人民币口径待 P7-1 currency 字段，现在接入=美元字段存人民币资损口径错误 → 延后 P7；aliyun 原 URL 已 404 待重定位（llms.txt 索引法待试）。fixture 扩至 51 项断言（xai 分档/÷1000/非 token 表、tencent 双风格变体/杂质过滤）；修复 fixture 尾部重复 echo/exit。回归：test-time-discounts 28/28、verify-ratios 无回归、pint PASS。
- 2026-09-12（五）：P1-2 再落 moonshot/minimax 两家解析器（现 9 厂商接通、0 失败），**llms.txt 索引法成为 SPA 探测标准动作**：moonshot（platform.moonshot.cn 301 → platform.kimi.com；`/docs/llms.txt` 直达索引 → `platform.kimi.ai/docs/pricing/chat.md`；表格=Mintlify JSX `<DocTable rows={[...]}>`，行内 `<>{"$"}</>` 价格元素 + `\"` JSON 转义 → 入口先归一化再匹配；**列序=缓存命中价在前**与 DeepSeek 相反；**注册国际站 kimi.ai 美元口径**，中文站 ¥ 不配——同 siliconflow 资损逻辑 → 真抓 4 模型**与库内预置零 diff，交叉验证通过**）、minimax（minimax.io/`pricing-paygo.md`；Standard/Priority 双 Tab → Priority 1.5x 条件价整段剥离、Legacy Accordion（M2.5/M2.1 退役）剥离、M3 ≤512k/>512k 分档取首档、划线促销 `~~$0.60~~ $0.30` Permanent 50% off 取实价 → 真抓 3 模型「新增 3」）。**aliyun 源重定位完成**：llms.txt（126KB）+ llms-full.txt（5.7MB）可抓，但**单页 .md 直取被 WAF 拦**（动态 `X-Request-Context` 头）；内容=Token Plan 个人/团队套餐档位（39/139/499 元 CNY，无按量价）→ 注册表改指 llms-full.txt 仅存快照，档位解析归 P2-1、币种归 P7-1。fixture 扩至 61 项断言（moonshot JSX/列序/上下文数字防污染、minimax 划线实价/Tab/Accordion 剥离/分档）。回归：fixture 61/61、pint PASS；待确认变更 121 处（moonshot 零 diff 未增）。
- 2026-09-12（六）：P1-2 最终 4 家 SPA 源探测收官——**llms.txt/.md 技巧在国内门户失效**，volcengine/baidu 落地、unicom/cmcc 留待：volcengine（doccenter garfish SPA 无内嵌数据、llms.txt 301 → **JS bundle 挖 API 法**：`grep '/api/doc/'` portal CDN main.js → `GET docs.volcengine.com/api/doc/getDocDetail?DocumentID=1544106&lang=zh`（无鉴权，需 `Referer: docs.volcengine.com/...`；旧 doc 1099320 已 301 → 1544106 model-pricing）；响应 Content=**Quill delta JSON**（529KB，zone 结构：Z 正文/R 行/C cell 引用（insert={id}）/x* cell 文本）→ 逐 zone 正则提 `doubao-*` → **VolcengineDocParser** 真抓 38 模型目录「新增 38」）、baidu（旧 doc QIANFAN/s/hlpl7xe2f 302 → 从 index 定位新页 `qianfan/s/wmh4sv6ya` 415KB 全 SSR、217 行价格表 → **BaiduParser**：「版本名称」列=API id 但连排无分隔（`ERNIE-5.1ERNIE-5.1-Speed-Preview`）→ `(?:ernie|bce)-` 前瞻 preg_split + modelName 白名单，量包表无版本名称表头自动跳过 → 真抓 31 模型目录（ERNIE + 托管 deepseek-v4/glm-5.x/kimi-k2.6/qwen3.5/OCR 系）「新增 30」）。**重要口径发现：P1-2 剩余国内厂商按量价全为 CNY**（volcengine 元/百万 token、baidu 元/千 tokens——后者单位天然 /千届时无需 ÷1000），与 siliconflow/aliyun 同构 → 按量三率统一等 P7-1 currency 字段，本轮均以 `catalog_from_pricing` 产目录（解析器 parsePricing 返回空 + 注释）。unicom 探测：1MB DedeCMS 模板壳正文纯 XHR（无 `__INITIAL_STATE__`，hostConfig.js 仅站点路径）→ API 未定位；cmcc：React `cloud-cms-service-web` 纯壳 1KB → CMS API 未定位；两家价目已有迁移 000006/000008 核对预置，待后续挖 JS 或人工。fixture 扩至 63 项断言（baidu 连排切分/量包表跳过/CNY 空 pricing、volcengine 双层 JSON 构造样本（json_encode 免手写转义）/逐 zone 提取/叙述句防整体入库/壳 HTML 安全空）。E2E：snapshot-only + 真抓 7 厂商 0 失败，目录 pending volcengine 38 + baidu 30；待确认变更累计 ~189 处。回归：fixture 63/63、pint PASS。
- 2026-09-12（十一）：**P7-3 折算与汇率留痕落地**——`CurrencyExchangeService` 新增 `convert(amount, from, to)`（以基准 CNY 中转双向，`amount×rate(from)/rate(to)`，任一端不可折算 null）+ `snapshotFor()`（快照 {base,from,to,rates,taken_at}，纯基准/不可折算返回 null）+ `vendorOfficialCurrency()`（memo 查 `coding_plan_vendors.currency`，缺失回落 CNY）；计费挂点：`CodingPlanPoolService::recordUsage` 当厂商官方币种非 CNY 时自动把 fx 快照写进 `coding_plan_usage_logs.meta.fx`（已有 meta 可显式覆盖；CNY 厂商零开销不留痕）——USD 厂商（openai/google/anthropic）用量从此可审计「当时若按官方价折算」所用汇率；订单侧暂无非基准折算发生点，预留同构。`CodingPlanVendor` fillable 补 `currency`（P7-1 遗漏）。新增自检命令 `coding-plan:test-currency`（21 项断言：三级回落/双向互逆/快照结构/vendor 币种/recordUsage 集成，事务内零残留）。回归：test-currency 21/21 ✓、time-discounts 28/28 ✓、pint PASS、自检临时行 0 残留 ✓。
- 2026-09-12（十二）：**P7-4 客户结算货币落地**——迁移 000012 `users.settlement_currency`（nullable，NULL=跟随平台基准 CNY，幂等守卫 + down 支持）+ `User::settlementCurrency()`（未设回落 CNY、存储归一大写）+ `CurrencyExchangeService::convertQuota(quota, currency)`（平台既有口径 QuotaPerUnit quota=1 美元单位 → convert() 折偏好币种，随附 fx 快照贯彻「结算口径=交易时刻汇率快照」；无偏好/即基准/不可折算 → null）+ `GET /user/self` 带 `settlement_currency` 与 `settlement` 换算块（报价/账单/余额展示层换算数据源，P7-5 前端消费）+ `PUT /user/self` 支持改偏好（空值=清除跟随平台、非空 isSupported 校验否则 422）。用户侧语义：quota 金额折算只做展示换算不落库，真实扣费仍是平台 quota 内部单位（vendor 官方价折算审计已在 P7-3 于用量 meta.fx 落地）。自检 test-currency 扩至【F】段 30 项断言（convertQuota 恒等/线性/快照/settlementCurrency 回落，事务内零残留）。回归：test-currency 30/30 ✓、time-discounts 28/28 ✓、migrate 000012 ✓、pint PASS、临时行 0 残留 ✓。


- 2026-09-12（十）：**P7-2 汇率配置落地**——迁移 000011 `currency_rates`（code 主键、rate=1 单位折人民币、source=manual/api、updated_at）+ `CurrencyRate` 模型 + `CurrencyExchangeService::rate()`（表值优先 → USD 无行回落 Option `UsdExchangeRate`=7.3 实测生效 → CNY 恒 1 → 其余 null+isSupported()）+ 管理端 API CRUD（`GET/POST/DELETE /coding_plan/rates`，upsert+flushMemo；rates 列表带 effective_rate/fallback 提示便于判断兜底状态）；vendor/tier CRUD validate 补 `currency`。回归：route:list 3 条 ✓、time-discounts 28/28 ✓、fixture ✓、pint PASS。
- 2026-09-12（九）：**P1-2 全部收官 + P7-1 落地**——①cmcc 终局：40 chunks 全查（`request-api/` 仅 resource/file、frontend-favorite 三类路径），带完整浏览器头重试 `/service-api/article` → **HTTP 302 → `iam/oidc/authorize?client_id=opgateway`**：文档读取 API 全部经 OIDC 认证网关、匿名不可抓（此前「curl 挂起」即未跟进 302 的黑洞表现）——端点存在、需登录态，预置数据为正确归宿；②**P7-1 迁移 000010**：`coding_plan_vendors.currency`（默认 CNY，openai/google/anthropic 预置 USD；xai 库内无厂商行）+ `coding_plan_vendor_tiers.currency`（NULL=继承厂商），解除 siliconflow 阻塞；③**SiliconflowParser**：`siliconflow.cn/pricing` SSR 直出（~225KB，Tailwind 静态），`<a title="org/model">` 抽全模态 id（`Pro/` 前缀=加速标记剥除、无斜杠/中文 title 跳过）→ 真抓 **48 模型目录「新增 48」**（zai-org/glm-5.3、deepseek-ai/deepseek-v4-pro、tencent/hunyuan 系等完整 API id）；按量价 ¥/M tokens（CNY，双时段「9点～18点」价组）→ 折算比率归 P7-2/P7-3。fixture 73 项断言、pint PASS、E2E + 真抓 0 失败；目录 pending 累计 ~266 处。
- 2026-09-12（八）：**新源接入：超算互联网 SCNet（中科曙光）Token Plan**——用户发现 `scnet.cn/ac/openapi/doc/2.0/moduleapi/plans/token-plan.html`；形态最友好的国内源：**VitePress 文档站 SSR 直出正文**（13.6KB，无需挖 JS/XHR）→ **ScnetParser**：按表头「模型ID」定位「可用模型」表列（品牌|模型ID|能力|协议，规则 4 列无 rowspan）→ modelName 白名单 → 真抓 **19 模型目录「新增 19」**（GLM-5.3 系、DeepSeek-V4/V4.1 系、Kimi-K3/K2.7-Code、MiniMax-M3、Qwen3.8 系等国产主流）。定价口径：套餐档位基础/标准/高级/旗舰（活动价 ¥30/110/265/764，原价 ¥50/185/440/1274，月度 Credits 额度 6 万~180 万）→ CNY Credits 量纲归 P2-1；Credits 扣减倍率表（如 GLM-5.3=2.29、Kimi-K3=4.12）→ 折算系数仅参考归 P7-1；parsePricing 返回空。fixture 70 项断言（scnet：列定位跳档位/倍率表、大小写归一、空页安全）；pint PASS；E2E + 真抓 0 失败；目录 pending 累计 ~218 处。
- 2026-09-12（七）：**unicom 探测反转落地**——重挖页面发现「16MB 内嵌数据」真相：**无任何 XHR**，DedeCMS 壳页面 JS 直接内嵌 `totalList = [...];` 全站文档树（16.5MB、113 篇文档，`documentEntityList[].content`=富文本 HTML，`id`=URL 的 arcid；7015=Coding Plan概述、7080=Token Plan概述）→ **UnicomParser**：①手写 JSON 深度状态机定位 JSON 结束（关键坑：JSON 结束后还有 27KB 其他 JS 才出现 `];`，按 `];` 截取会让 PHP json_decode 因 trailing data 报 Syntax error——python raw_decode 无此问题故上轮误判「无法解析」）②BFS 抽 7015/7080 两篇「支持模型」列 ③基类新增 `prepareCellHtml` 钩子（默认恒等）：unicom 格内模型为独立 `<p>` 段（`</p><p>` 无空白）→ 覆盖为块级标签换行防 cleanText 粘连（deepseek-v4-flashglm-5.1）④顿号/空白拆分 + 「别名：说明」取冒号前段（aisp-auto-route：智能路由… → aisp-auto-route）+ modelName 白名单 → 真抓 **11 模型目录「新增 10」**（aisp-auto-route 命中迁移 000006 预置）。定价口径：Coding Plan 档位 Lite 40/Pro 200 元/月（次数额度）、Token Plan 个人 15/30/45 + 团队 198/698/1398 元/月（credits）→ CNY 归 P2-1；credits 折算「综合单价 元/百万 tokens」仅参考 → P7-1。**cmcc 进展**：抓全 40 个 webpack chunk → API 模式定位 `/api/web/op-help-center/request-api/{record,service}-api/…`（chunk 内可见 frontend-favorite），`getArticleAndProductRecommends` 定义 chunk 不在静态列表，`/service-api/article` 变体 curl 挂起（WAF drop）→ 端点未命中，依赖预置。fixture 67 项断言全过（含 unicom：内嵌树提取/注行防污染/壳页安全空/parsePricing 空）；pint PASS；E2E snapshot-only + 真抓 6/6 厂商 0 失败；目录 pending 累计 ~199 处。

